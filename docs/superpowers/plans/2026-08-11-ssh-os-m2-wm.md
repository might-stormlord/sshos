# ssh_os 2.0 — Jalon 2 : le gestionnaire de fenêtres, le panneau et le menu

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Remplacer la boîte colorée du jalon 1 par un vrai bureau : des fenêtres décorées qu'on déplace et redimensionne à la souris et au clavier, un panneau sur le bord choisi, un menu de lancement, et deux applications factices qui exercent tout le contrat applicatif.

**Architecture:** Le gestionnaire de fenêtres ne connaît ni descripteur de fichier ni `epoll` : il manipule des rectangles et une pile. `src/daemon/host.cpp` est le seul pont entre une application et la boucle d'événements, via deux coutures injectées — `FdRegistrar` (le démon donne à la session l'accès à son `epoll`) et `Host` (la session donne à une application le droit de surveiller un descripteur, de changer son titre, de demander sa fermeture). Chaque `watch()` reçoit une clé générationnelle : un événement en retard sur un descripteur recyclé est jeté sans bruit au lieu d'être livré au mauvais destinataire.

**Tech Stack:** C++20, CMake, glibc/Linux. Aucune bibliothèque externe. Aucun nouveau descripteur de type inédit : `epoll`, `timerfd` et `signalfd` du jalon 1 suffisent.

**Spec de référence :** `docs/superpowers/specs/2026-08-11-jalon-2-wm-design.md` (conception du jalon), adossée à `docs/superpowers/specs/2026-08-10-ssh-os-design.md` (conception maître, §5 à §8). Le plan du jalon 1, `docs/superpowers/plans/2026-08-10-ssh-os-m1-noyau.md`, décrit le socle sur lequel tout ceci s'appuie.

## Global Constraints

Ces contraintes valent pour **toutes** les tâches et ne sont pas répétées dans chacune.

- **C++20**, `set(CMAKE_CXX_EXTENSIONS OFF)`. Options : `-Wall -Wextra -Wpedantic -Werror`. `Debug` ajoute `-fsanitize=address,undefined -g -O0` ; `Release` compile en `-O2`.
- **Conséquence directe de `-Wpedantic -Werror` : `\e` est une extension GCC et ne compile pas.** Écrire `\033` dans tous les littéraux. Une seule occurrence de `\e` casse la compilation du projet entier.
- **Zéro dépendance externe.** Uniquement la bibliothèque standard et l'API POSIX/Linux.
- **Ne pas modifier `CMakeLists.txt`.** Il globe déjà `src/*.cpp` en récursif (`GLOB_RECURSE ... CONFIGURE_DEPENDS`) et `tests/test_*.cpp` : tout fichier créé aux bons endroits est pris automatiquement. Un nouveau répertoire sous `src/` ne demande aucune déclaration.
- **Un thread, un `epoll`, aucun mutex.** Aucune commande détachée (`nohup`, `&`, `disown`) nulle part, y compris dans les tests.
- Tous les descripteurs sont ouverts `CLOEXEC`. Toutes les écritures sont non bloquantes.
- **`unwatch()` précède toujours `close()`.** L'ordre inverse laisse une entrée `epoll` sur un numéro de descripteur que le noyau peut réattribuer à la milliseconde suivante.
- Le français est la langue des messages destinés à l'utilisateur **et des commentaires** ; le code et les identifiants sont en anglais. Les commentaires portent le *pourquoi*, pas le *quoi*.
- Les messages de commit sont **sans accents** (convention du dépôt), contrairement aux commentaires de code.
- Valeurs figées du projet, à ne jamais recalculer ni « améliorer » : rendu **33 ms**, contre-pression **1 Mo**, plafond **64 fenêtres**, ambiguïté `ESC` **50 ms**, leader **`Ctrl+A`**, plancher d'une fenêtre **16×5**, zone de travail minimale **40×12**.
- **Jamais de processus identifié par son nom.** `ps`/`pgrep -f` matchent leur propre ligne de commande ; un test qui cherche `sshos` se trouve lui-même. Toujours un `pid` obtenu par `fork()`.

## Commandes de référence

```bash
cmake --build build-release -j"$(nproc)"     # compiler en Release
./build-release/sshos_tests                  # toute la suite
./build-release/sshos_tests decor            # seulement les cas dont le nom contient "decor"

cmake --build build-dbg -j"$(nproc)"         # ASan + UBSan
UBSAN_OPTIONS=halt_on_error=1:print_stacktrace=1 ./build-dbg/sshos_tests
```

**Ne jamais filtrer la sortie sur `grep -i FAIL`** : plusieurs cas portent `failed` dans leur nom (`..._after_failed_explicit_release`) et le filtre les compte comme des échecs. Lire la **ligne de bilan** finale (`N cas, M en echec`).

Point de départ : **189 cas, 0 en echec**, en `Release` comme en `Debug`.

---

## Structure des fichiers

| Fichier | Responsabilité |
|---|---|
| `src/render/theme.hpp/.cpp` | Palette nommée, une par profondeur de couleur ; `Border` vit dans `cell.hpp` |
| `src/render/profile.hpp/.cpp` | *(modifié)* expose `quantize_color()`, jusqu'ici enfermée dans l'anonyme |
| `src/render/surface.hpp/.cpp` | *(modifié)* ajoute `View::box()` |
| `src/render/cell.hpp` | *(modifié)* ajoute `enum class Border` |
| `src/app/app.hpp` | Contrat applicatif : `App`, `Host`, `CloseCheck`, `IoStatus` |
| `src/app/catalog.hpp/.cpp` | Table des applications lançables (identifiant, libellé, fabrique) |
| `src/apps/bloc.hpp/.cpp` | Application factice sans descripteur : taille, curseur, titre, `can_close` |
| `src/apps/battement.hpp/.cpp` | Application factice **avec** descripteur : tuyau surveillé, `on_io`, mort propre |
| `src/wm/window.hpp/.cpp` | `Window`, `WinMode`, géométrie du cadre, `frame_min`, `clamp_to` |
| `src/wm/decor.hpp/.cpp` | Métrique et dessin des décorations : titre, boutons, bordures, poignée |
| `src/wm/hittest.hpp/.cpp` | Point → zone d'une fenêtre. Inverse exact de `decor` |
| `src/wm/manager.hpp/.cpp` | Pile, focus, ouverture, fermeture, cascade, aimantation, modes |
| `src/wm/layout.hpp/.cpp` | Zone de travail, `user_rect` → `display_rect`, réversibilité au redimensionnement |
| `src/shell/panel.hpp/.cpp` | Panneau : bord, épaisseur, bouton de menu, épinglées, tâches, débordement |
| `src/shell/clock.hpp/.cpp` | Horloge du panneau, salissure seulement quand la chaîne change |
| `src/shell/menu.hpp/.cpp` | Menu de lancement : catalogue, filtre, commandes de session |
| `src/shell/modal.hpp/.cpp` | Dialogue modal : question, Annuler / Confirmer, capture du focus |
| `src/input/shortcuts.hpp/.cpp` | `LeaderDispatch` : `Ctrl+A` puis une touche → `Action` |
| `src/daemon/host.hpp/.cpp` | `FdRegistrar` (couture vers l'`epoll`) et `HostImpl` (clés générationnelles) |
| `src/daemon/session.hpp/.cpp` | *(récrit)* le bureau : compose, route les entrées, tient la MAE de glissement |
| `src/daemon/daemon.cpp` | *(modifié)* clés `u64` au lieu de `data.fd`, câblage du `FdRegistrar` |

### Règle de dépendance

```
render  → (rien)
input   → (rien)
app     → render
wm      → render, app
shell   → wm, render
apps/*  → render, app
daemon  → tout
```

`wm/` et `shell/` ne connaissent **ni descripteur ni `epoll`**. Toute inclusion de `<sys/epoll.h>` sous `src/wm/` ou `src/shell/` est un défaut de conception, pas un détail.

### Correspondance avec le §13 de la spec

La spec ordonne la construction en 9 étapes. Ce plan en compte **11** : son étape 1 (« une fenêtre décorée avec une vraie app + panneau statique ») est découpée en quatre tâches, parce qu'elle mélange quatre couches indépendantes dont trois sont testables sans démon. Ses étapes 2 à 6 deviennent les tâches 5 à 9 dans le même ordre. Ses étapes 7 à 9 se redistribuent en deux tâches plutôt que trois : le menu part avec les raccourcis (le menu s'ouvre par une `Action`, les séparer forcerait à écrire deux fois le même câblage), et la modale part avec les goldens et la revue.

| Tâche du plan | Étape §13 |
|---|---|
| 1 · Thème, quantification, `View::box` | 1 (partie rendu) |
| 2 · Contrat applicatif, catalogue, Bloc | 1 (partie application) |
| 3 · `Window`, `frame_min`, décorations | 1 (partie wm) |
| 4 · `Session` recomposée, migration des tests | 1 (partie démon) — **le squelette vertical devient visible** |
| 5 · Hit-test, glissement, sept annulations | 2 |
| 6 · Pile, focus, modes, cascade, aimantation, plafond | 3 |
| 7 · Disposition, réversibilité, préservation | 4 |
| 8 · `FdRegistrar`, clés générationnelles, Battement | 5 |
| 9 · Panneau complet et horloge | 6 |
| 10 · Menu et raccourcis leader | 7 + 8 (partie clavier) |
| 11 · Modale, goldens, revue de jalon | 8 (partie fermeture) + 9 |

Une remarque sur ce découpage : la conversion de l'`epoll` du démon aux clés `u64` (aujourd'hui `epoll_event.data.fd`) est **repoussée à la tâche 8**, où elle sert. Rien avant elle n'enregistre de descripteur applicatif, et `daemon.cpp` est le fichier le plus délicat du projet — il sort tout juste du round EPOLLHUP. La tâche 4 n'y touche donc qu'en un point, le câblage du profil de sortie.

---

## La migration des tests du bouchon — à lire avant la tâche 4

`src/daemon/session.cpp` est un bouchon de 123 lignes et **douze tests s'appuient sur ce qu'il dessine**. Les remplacer à l'aveugle perdrait des propriétés durement acquises (la preuve de l'heure d'été, la discrimination des deux formulations de l'avertissement, les trois tests du round EPOLLHUP). La tâche 4 les migre selon la règle suivante, choisie pour que **quatre aiguilles seulement** changent.

**Le bouton de menu du panneau s'écrit `☰ ssh_os` quand le client annonce UTF-8, et `ssh_os` sinon.** La marque reste donc présente dans le panneau sur les deux profils. Toutes les assertions existantes qui cherchent `"ssh_os"` dans la dernière ligne survivent **verbatim**, en test unitaire (profil par défaut, non-UTF-8) comme en bout-en-bout (les cinq tests concernés envoient `hello.utf8 = true`).

**Bloc affiche `clics: N`.** Les trois tests du round EPOLLHUP comptent des clics et vérifient qu'ils survivent au rattachement ; l'observable équivalente au jalon 2 est le compteur de l'application factice. Les aiguilles `"clics: 0"` et `"clics: 3"` survivent verbatim, à condition que le clic atteigne Bloc — voir ci-dessous.

**La géométrie de la première fenêtre est déterministe.** `Rect{2, 1, 44, 14}` ramené dans la zone de travail. Sur un terminal 80×24, la zone de travail vaut `{0, 0, 80, 23}` (le panneau prend la dernière ligne), le cadre reste `{2, 1, 44, 14}` et la zone cliente `{3, 2, 42, 12}`. Les trois tests EPOLLHUP envoient `\033[<0;10;5M` : coordonnées SGR 1-indexées, donc **(9, 4) en 0-indexé**, franchement à l'intérieur de la zone cliente. Le clic arrive à Bloc sans qu'aucun de ces tests ne change.

Les quatre seules aiguilles qui bougent, toutes `"ssh_os 2.0"` → `"Bloc"` :

| Test | Ligne | Changement |
|---|---|---|
| `session_draws_a_bordered_box_with_its_title` | 220 | renommé `session_draws_a_decorated_window_with_its_title`, aiguille `"Bloc"` |
| `session_geometry_follows_the_surface_not_the_constructor_arguments` | 306, 316 | aiguille `"Bloc"` |
| `end_to_end_attach_render_detach_kill` | 894 | `wait_for_frame_containing(..., "Bloc", "ssh_os", 2000)` |
| `daemon_mute_probe_does_not_evict_the_attached_client` | 1323 | idem |

Restent **inchangés** : `session_draws_a_panel_on_the_last_row`, `session_clock_follows_daylight_saving_under_a_single_timezone`, `session_survives_a_terminal_smaller_than_the_minimum`, `session_quits_on_ctrl_q`, `session_shows_full_warning_when_width_permits_but_height_does_not`, `session_shows_short_warning_when_width_is_too_small_for_the_full_message`, et les trois tests EPOLLHUP.

---

## Tâches

### Task 1: Thème, quantification exposée, `View::box`

Trois briques de rendu pur, sans démon ni fenêtre. La moins évidente des trois est la quantification : `profile.cpp` sait déjà réduire une couleur à 256 ou 16 teintes **au moment d'émettre le SGR**, mais cette fonction est enfermée dans un `namespace` anonyme. Le thème a besoin de la même réduction pour une raison différente — savoir si deux teintes qu'il veut distinctes vont **s'effondrer sur la même** en 16 couleurs. On l'expose donc plutôt que de la réécrire.

Ce qu'on découvre en la regardant : `quantize_16` teste chaque canal contre 127 et n'utilise pas le bit brillant, donc en `Mono16` **seules huit couleurs sont atteignables**, et toute teinte sombre — un fond de bureau, un fond de panneau, une barre de titre inactive — s'effondre sur le noir. Dériver les palettes basses par quantification rendrait la structure de l'interface invisible exactement sur les terminaux qui en ont le plus besoin. Le thème porte donc **trois palettes explicites**, et la fonction exposée sert à *prouver par test* qu'aucune ne s'effondre.

**Files:**
- Modify: `src/render/profile.hpp`, `src/render/profile.cpp`
- Modify: `src/render/cell.hpp`
- Create: `src/render/theme.hpp`, `src/render/theme.cpp`
- Modify: `src/render/surface.hpp`, `src/render/surface.cpp`
- Create: `tests/test_theme.cpp`
- Modify: `tests/test_surface.cpp`

**Interfaces:**
- Consumes: `Color`, `ColorKind`, `Style`, `Rect` (`render/cell.hpp`) ; `OutputProfile`, `ColorDepth` (`render/profile.hpp`) ; `View::put` (`render/surface.hpp`).
- Produces:
  - `enum class sshos::Border { Unicode, Ascii };` dans `render/cell.hpp`.
  - `sshos::Color sshos::quantize_color(const Color& c, ColorDepth d)` — la couleur telle qu'elle sera **réellement affichée** sur cette profondeur. `Default` reste `Default` ; en `TrueColor` la couleur est rendue telle quelle ; sinon le résultat est toujours `ColorKind::Indexed`.
  - `struct sshos::Theme` avec les douze champs `Color` : `desktop_bg`, `panel_bg`, `panel_fg`, `accent`, `title_focus_bg`, `title_focus_fg`, `title_blur_bg`, `title_blur_fg`, `border_focus`, `border_blur`, `modal_bg`, `modal_fg` ; et `static Theme defaults()`, `static Theme indexed256()`, `static Theme mono16()`, `Theme for_profile(const OutputProfile& p) const`.
  - `void sshos::View::box(Rect r, Border b, Style st)` — dessine **le contour seulement**, jamais l'intérieur.

- [ ] **Step 1: Écrire les tests qui échouent**

Créer `tests/test_theme.cpp` :

```cpp
#include "harness.hpp"
#include "render/profile.hpp"
#include "render/theme.hpp"

using sshos::Color;
using sshos::ColorDepth;
using sshos::ColorKind;
using sshos::OutputProfile;
using sshos::Theme;

TEST(quantize_color_leaves_truecolor_untouched) {
  const Color c = Color::rgb(0x2e, 0x74, 0xb5);
  CHECK_EQ(sshos::quantize_color(c, ColorDepth::TrueColor), c);
}

TEST(quantize_color_maps_rgb_into_the_216_cube) {
  const Color q = sshos::quantize_color(Color::rgb(255, 0, 0), ColorDepth::Indexed256);
  CHECK(q.kind == ColorKind::Indexed);
  CHECK_EQ(static_cast<int>(q.idx), 196);  // 16 + 36*5 + 6*0 + 0
}

TEST(quantize_color_keeps_an_indexed_color_indexed_on_256) {
  const Color q = sshos::quantize_color(Color::indexed(129), ColorDepth::Indexed256);
  CHECK_EQ(static_cast<int>(q.idx), 129);
}

// La propriété qui motive les trois palettes : en 16 couleurs, tout canal
// sous 128 tombe à zéro. Deux gris sombres différents deviennent le meme
// noir -- c'est ce que le thème ne doit jamais laisser arriver.
TEST(quantize_color_collapses_every_dark_tone_onto_black) {
  const Color a = sshos::quantize_color(Color::rgb(0x18, 0x20, 0x30), ColorDepth::Mono16);
  const Color b = sshos::quantize_color(Color::rgb(0x2c, 0x32, 0x40), ColorDepth::Mono16);
  CHECK_EQ(static_cast<int>(a.idx), 0);
  CHECK_EQ(static_cast<int>(b.idx), 0);
}

TEST(quantize_color_leaves_the_default_color_alone) {
  const Color q = sshos::quantize_color(Color::def(), ColorDepth::Mono16);
  CHECK(q.kind == ColorKind::Default);
}

TEST(theme_for_profile_selects_one_palette_per_depth) {
  const Theme t = Theme::defaults();
  const OutputProfile mono{ColorDepth::Mono16, false};
  const OutputProfile idx{ColorDepth::Indexed256, true};
  const OutputProfile full{ColorDepth::TrueColor, true};
  CHECK(t.for_profile(mono).desktop_bg == Theme::mono16().desktop_bg);
  CHECK(t.for_profile(idx).desktop_bg == Theme::indexed256().desktop_bg);
  CHECK(t.for_profile(full).desktop_bg == Theme::defaults().desktop_bg);
}

// LE test du thème. Chaque paire ci-dessous porte une distinction que
// l'utilisateur doit voir : sans elle, une fenêtre active ne se distingue
// plus d'une inactive, ou le panneau se fond dans le bureau. On vérifie la
// distinction sur la couleur RÉELLEMENT affichée, donc après quantification.
TEST(theme_keeps_every_meaningful_distinction_on_every_depth) {
  const ColorDepth depths[] = {ColorDepth::Mono16, ColorDepth::Indexed256,
                               ColorDepth::TrueColor};
  for (ColorDepth d : depths) {
    OutputProfile p;
    p.depth = d;
    const Theme t = Theme::defaults().for_profile(p);
    const auto q = [d](Color c) { return sshos::quantize_color(c, d); };

    CHECK(q(t.border_focus) != q(t.border_blur));
    CHECK(q(t.title_focus_bg) != q(t.title_blur_bg));
    CHECK(q(t.title_focus_fg) != q(t.title_focus_bg));
    CHECK(q(t.title_blur_fg) != q(t.title_blur_bg));
    CHECK(q(t.panel_bg) != q(t.desktop_bg));
    CHECK(q(t.panel_fg) != q(t.panel_bg));
    CHECK(q(t.modal_bg) != q(t.desktop_bg));
    CHECK(q(t.modal_fg) != q(t.modal_bg));
  }
}
```

Ajouter à la fin de `tests/test_surface.cpp` :

```cpp
TEST(view_box_draws_a_unicode_frame_without_touching_the_inside) {
  Surface s(6, 4);
  View v = s.root();
  v.box(Rect{0, 0, 6, 4}, sshos::Border::Unicode, Style{});
  CHECK_EQ(s.at(0, 0).ch, U'\u250c');
  CHECK_EQ(s.at(5, 0).ch, U'\u2510');
  CHECK_EQ(s.at(0, 3).ch, U'\u2514');
  CHECK_EQ(s.at(5, 3).ch, U'\u2518');
  CHECK_EQ(s.at(3, 0).ch, U'\u2500');
  CHECK_EQ(s.at(0, 2).ch, U'\u2502');
  CHECK_EQ(s.at(5, 2).ch, U'\u2502');
  CHECK_EQ(s.at(3, 2).ch, U' ');
}

TEST(view_box_falls_back_to_ascii) {
  Surface s(4, 3);
  View v = s.root();
  v.box(Rect{0, 0, 4, 3}, sshos::Border::Ascii, Style{});
  CHECK_EQ(s.at(0, 0).ch, U'+');
  CHECK_EQ(s.at(3, 2).ch, U'+');
  CHECK_EQ(s.at(1, 0).ch, U'-');
  CHECK_EQ(s.at(0, 1).ch, U'|');
}

// box() n'est pas une primitive privilégiée : elle passe par put(), donc
// elle hérite du clip et ne peut pas peindre chez la voisine.
TEST(view_box_clips_like_every_other_write) {
  Surface s(6, 4);
  View v = s.root().sub(Rect{1, 1, 3, 2});
  v.box(Rect{-2, -2, 20, 20}, sshos::Border::Ascii, Style{});
  for (int x = 0; x < 6; ++x) CHECK_EQ(s.at(x, 0).ch, U' ');
  for (int y = 0; y < 4; ++y) CHECK_EQ(s.at(0, y).ch, U' ');
  CHECK_EQ(s.at(4, 1).ch, U' ');
}

TEST(view_box_degenerates_without_crashing) {
  Surface s(5, 3);
  View v = s.root();
  v.box(Rect{0, 0, 5, 1}, sshos::Border::Ascii, Style{});
  CHECK_EQ(s.at(0, 0).ch, U'-');
  CHECK_EQ(s.at(4, 0).ch, U'-');
  v.box(Rect{0, 1, 1, 2}, sshos::Border::Ascii, Style{});
  CHECK_EQ(s.at(0, 1).ch, U'|');
  CHECK_EQ(s.at(0, 2).ch, U'|');
  v.box(Rect{2, 1, 0, 0}, sshos::Border::Ascii, Style{});
  CHECK_EQ(s.at(2, 1).ch, U' ');
}
```

- [ ] **Step 2: Lancer pour vérifier que ça échoue**

```bash
cmake --build build-release -j"$(nproc)"
```

Attendu : ÉCHEC de compilation, `render/theme.hpp: No such file or directory` et `'class sshos::View' has no member named 'box'`.

- [ ] **Step 3: Exposer `quantize_color` dans `profile.hpp`**

Ajouter à `src/render/profile.hpp`, après la déclaration de `OutputProfile` :

```cpp
// Rend la couleur telle qu'elle sera RÉELLEMENT affichée sur cette
// profondeur. Exposée parce que le thème (render/theme.hpp) doit pouvoir
// vérifier que deux teintes qu'il veut distinctes ne s'effondrent pas sur
// la même une fois réduites -- une question que seul l'appelant peut poser,
// et à laquelle sgr_transition() répond trop tard, au moment d'émettre.
Color quantize_color(const Color& c, ColorDepth d);
```

- [ ] **Step 4: Implémenter `quantize_color` et réécrire `color_code` par-dessus**

Dans `src/render/profile.cpp`, **après** la fermeture du `namespace` anonyme — donc après `}  // namespace` ligne 69 — ajouter la définition, puis remplacer le corps de `color_code`. Les trois fonctions `quantize_256`, `quantize_16` et `indexed_to_rgb` restent où elles sont, dans l'anonyme.

`color_code` étant elle-même dans l'anonyme et `quantize_color` en dehors, il faut déplacer `color_code` **après** `quantize_color`, ou déclarer cette dernière plus haut. Le plus simple : garder `color_code` dans l'anonyme et déclarer `quantize_color` en avant, juste sous `namespace sshos {`, ligne 7 :

```cpp
Color quantize_color(const Color& c, ColorDepth d);  // définie plus bas
```

Nouveau corps de `color_code` :

```cpp
std::string color_code(const Color& c, bool foreground, const OutputProfile& p) {
  const int base = foreground ? 38 : 48;
  const int simple = foreground ? 30 : 40;
  const int reset = foreground ? 39 : 49;

  if (c.kind == ColorKind::Default) return "\033[" + num(reset) + "m";

  if (p.depth == ColorDepth::TrueColor) {
    uint8_t r = c.r;
    uint8_t g = c.g;
    uint8_t b = c.b;
    if (c.kind == ColorKind::Indexed) indexed_to_rgb(c.idx, r, g, b);
    return "\033[" + num(base) + ";2;" + num(r) + ";" + num(g) + ";" + num(b) + "m";
  }

  // Une seule règle de réduction dans tout le projet : celle de
  // quantize_color. Le thème et l'émetteur SGR doivent répondre la même
  // chose, sinon le thème « prouve » une distinction que l'écran ne montre
  // pas.
  const Color q = quantize_color(c, p.depth);
  if (p.depth == ColorDepth::Indexed256)
    return "\033[" + num(base) + ";5;" + num(q.idx) + "m";
  return "\033[" + num(simple + q.idx) + "m";
}
```

Et la définition, après le `}  // namespace` de l'anonyme :

```cpp
Color quantize_color(const Color& c, ColorDepth d) {
  if (c.kind == ColorKind::Default) return c;
  if (d == ColorDepth::TrueColor) return c;

  uint8_t r = c.r;
  uint8_t g = c.g;
  uint8_t b = c.b;
  if (c.kind == ColorKind::Indexed) indexed_to_rgb(c.idx, r, g, b);

  if (d == ColorDepth::Indexed256) {
    // Un index reste lui-même : le terminal en connaît déjà les 256, le
    // faire transiter par le cube 6x6x6 ne ferait que le dégrader.
    if (c.kind == ColorKind::Indexed) return c;
    return Color::indexed(quantize_256(r, g, b));
  }
  return Color::indexed(static_cast<uint8_t>(quantize_16(r, g, b)));
}
```

- [ ] **Step 5: Ajouter `Border` à `cell.hpp`**

Dans `src/render/cell.hpp`, juste avant `struct Cell` :

```cpp
// Jeu de caractères des encadrements. Il vit ici plutôt que dans theme.hpp
// pour que surface.hpp, qui déclare View::box(), n'ait pas à tirer
// profile.hpp derrière lui.
enum class Border { Unicode, Ascii };
```

- [ ] **Step 6: Écrire `theme.hpp` et `theme.cpp`**

`src/render/theme.hpp` :

```cpp
#pragma once

#include "render/cell.hpp"
#include "render/profile.hpp"

namespace sshos {

// Palette nommée du bureau. Chaque champ porte un rôle, pas une teinte :
// c'est ce qui permet d'en écrire trois versions sans que le reste du code
// ne sache laquelle il utilise.
struct Theme {
  Color desktop_bg;
  Color panel_bg;
  Color panel_fg;
  Color accent;
  Color title_focus_bg;
  Color title_focus_fg;
  Color title_blur_bg;
  Color title_blur_fg;
  Color border_focus;
  Color border_blur;
  Color modal_bg;
  Color modal_fg;

  // Trois palettes écrites à la main, et non deux dérivées de la première.
  // En 16 couleurs, quantize_color() ramène tout canal sous 128 à zéro :
  // fond de bureau, fond de panneau et titre inactif tomberaient tous les
  // trois sur le même noir, et la structure de l'interface disparaîtrait
  // exactement sur les terminaux les plus pauvres. Le test
  // theme_keeps_every_meaningful_distinction_on_every_depth garde ces trois
  // palettes honnêtes.
  static Theme defaults();     // 24 bits
  static Theme indexed256();
  static Theme mono16();

  Theme for_profile(const OutputProfile& p) const;
};

}  // namespace sshos
```

`src/render/theme.cpp` :

```cpp
#include "render/theme.hpp"

namespace sshos {

Theme Theme::defaults() {
  Theme t;
  t.desktop_bg = Color::rgb(0x20, 0x30, 0x50);
  t.panel_bg = Color::rgb(0x0c, 0x0e, 0x14);
  t.panel_fg = Color::rgb(0xc8, 0xd0, 0xdc);
  t.accent = Color::rgb(0x4a, 0x9e, 0xff);
  t.title_focus_bg = Color::rgb(0x1e, 0x5a, 0xa8);
  t.title_focus_fg = Color::rgb(0xff, 0xff, 0xff);
  t.title_blur_bg = Color::rgb(0x2c, 0x32, 0x40);
  t.title_blur_fg = Color::rgb(0x9a, 0xa4, 0xb4);
  t.border_focus = Color::rgb(0x4a, 0x9e, 0xff);
  t.border_blur = Color::rgb(0x50, 0x58, 0x68);
  t.modal_bg = Color::rgb(0x28, 0x2e, 0x3c);
  t.modal_fg = Color::rgb(0xf0, 0xf4, 0xfa);
  return t;
}

Theme Theme::indexed256() {
  Theme t;
  t.desktop_bg = Color::indexed(17);
  t.panel_bg = Color::indexed(234);
  t.panel_fg = Color::indexed(252);
  t.accent = Color::indexed(75);
  t.title_focus_bg = Color::indexed(25);
  t.title_focus_fg = Color::indexed(231);
  t.title_blur_bg = Color::indexed(238);
  t.title_blur_fg = Color::indexed(145);
  t.border_focus = Color::indexed(75);
  t.border_blur = Color::indexed(240);
  t.modal_bg = Color::indexed(236);
  t.modal_fg = Color::indexed(255);
  return t;
}

Theme Theme::mono16() {
  // Seuls les index 0 à 7 sont atteignables : quantize_16() n'utilise pas le
  // bit brillant. Choisir un index 8-15 ici donnerait une couleur qui
  // s'effondre silencieusement sur son homologue sombre.
  Theme t;
  t.desktop_bg = Color::indexed(4);
  t.panel_bg = Color::indexed(0);
  t.panel_fg = Color::indexed(7);
  t.accent = Color::indexed(6);
  t.title_focus_bg = Color::indexed(6);
  t.title_focus_fg = Color::indexed(0);
  t.title_blur_bg = Color::indexed(0);
  t.title_blur_fg = Color::indexed(7);
  t.border_focus = Color::indexed(7);
  t.border_blur = Color::indexed(4);
  t.modal_bg = Color::indexed(7);
  t.modal_fg = Color::indexed(0);
  return t;
}

Theme Theme::for_profile(const OutputProfile& p) const {
  switch (p.depth) {
    case ColorDepth::TrueColor:
      return *this;
    case ColorDepth::Indexed256:
      return indexed256();
    case ColorDepth::Mono16:
      return mono16();
  }
  return *this;
}

}  // namespace sshos
```

- [ ] **Step 7: Implémenter `View::box`**

Déclarer dans `src/render/surface.hpp`, dans la partie publique de `View`, juste après `fill` :

```cpp
  void box(Rect r, Border b, Style st);
```

Définir dans `src/render/surface.cpp` :

```cpp
void View::box(Rect r, Border b, Style st) {
  if (r.w <= 0 || r.h <= 0) return;

  const bool uni = (b == Border::Unicode);
  const char32_t hor = uni ? U'\u2500' : U'-';
  const char32_t ver = uni ? U'\u2502' : U'|';

  // Les cas dégénérés ne sont pas des erreurs : un cadre d'une ligne ou
  // d'une colonne arrive naturellement quand une fenêtre est réduite au
  // plancher, et planter là serait un défaut de robustesse, pas une
  // protection.
  if (r.h == 1) {
    for (int x = 0; x < r.w; ++x) put(r.x + x, r.y, hor, st);
    return;
  }
  if (r.w == 1) {
    for (int y = 0; y < r.h; ++y) put(r.x, r.y + y, ver, st);
    return;
  }

  for (int x = 1; x < r.w - 1; ++x) {
    put(r.x + x, r.y, hor, st);
    put(r.x + x, r.y + r.h - 1, hor, st);
  }
  for (int y = 1; y < r.h - 1; ++y) {
    put(r.x, r.y + y, ver, st);
    put(r.x + r.w - 1, r.y + y, ver, st);
  }
  put(r.x, r.y, uni ? U'\u250c' : U'+', st);
  put(r.x + r.w - 1, r.y, uni ? U'\u2510' : U'+', st);
  put(r.x, r.y + r.h - 1, uni ? U'\u2514' : U'+', st);
  put(r.x + r.w - 1, r.y + r.h - 1, uni ? U'\u2518' : U'+', st);
}
```

- [ ] **Step 8: Lancer les tests**

```bash
cmake --build build-release -j"$(nproc)" && ./build-release/sshos_tests
```

Attendu : **200 cas, 0 en echec**. Vérifier en particulier que **les cas de `test_profile.cpp` passent sans avoir été touchés** : c'est la preuve que la réécriture de `color_code` par-dessus `quantize_color` n'a rien changé au comportement observable.

- [ ] **Step 9: Passer les sanitizers**

```bash
cmake --build build-dbg -j"$(nproc)" && \
  UBSAN_OPTIONS=halt_on_error=1:print_stacktrace=1 ./build-dbg/sshos_tests
```

Attendu : même bilan, aucun rapport ASan/UBSan.

- [ ] **Step 10: Commit**

```bash
git add src/render/theme.hpp src/render/theme.cpp src/render/profile.hpp \
        src/render/profile.cpp src/render/cell.hpp src/render/surface.hpp \
        src/render/surface.cpp tests/test_theme.cpp tests/test_surface.cpp
git commit -m "feat(render): theme a trois palettes, quantification exposee, View::box"
```

---

### Task 2: Contrat applicatif, catalogue, l'application Bloc

Le contrat que toutes les applications du projet respecteront jusqu'au jalon 6. Il tient en deux interfaces : `App`, ce qu'une application doit savoir faire, et `Host`, ce qu'elle a le droit de demander en retour. Une application ne voit **jamais** de descripteur, de numéro de fenêtre ni d'`epoll` : elle reçoit une `View` déjà clippée sur sa zone cliente, et un jeton opaque quand elle demande à surveiller un descripteur.

Bloc est la première application réelle. Elle n'a aucune utilité pour l'utilisateur ; son travail est d'exercer chaque méthode du contrat pour que les tests aient prise sur elles.

**Files:**
- Create: `src/app/app.hpp`
- Create: `src/app/catalog.hpp`, `src/app/catalog.cpp`
- Create: `src/apps/bloc.hpp`, `src/apps/bloc.cpp`
- Create: `tests/test_app.cpp`

**Interfaces:**
- Consumes: `View`, `Size`, `Pos`, `Style`, `Color` (tâche 1 et jalon 1) ; `KeyEvent`, `MouseEvent`, `Key`, `mod`, `MouseAction` (`input/events.hpp`).
- Produces:
  - `struct sshos::CloseCheck { bool allowed; std::string question; static CloseCheck allow(); static CloseCheck ask(std::string); };`
  - `enum class sshos::IoStatus { Ok, Closed };`
  - `class sshos::Host` : `virtual void set_title(std::string)`, `virtual void request_close()`, `virtual void invalidate()`, `virtual uint64_t watch(int fd, uint32_t events)`, `virtual void unwatch(uint64_t token)`.
  - `class sshos::App` : `virtual void attach(Host&)`, `virtual void render(View)`, `virtual void on_key(const KeyEvent&)`, `virtual void on_mouse(const MouseEvent&)`, `virtual void on_resize(Size)`, `virtual IoStatus on_io(uint64_t token, uint32_t events)`, `virtual bool wants_cursor(Pos& out) const`, `virtual Size min_size() const`, `virtual CloseCheck can_close() const`.
  - `struct sshos::CatalogEntry { std::string id; std::string label; std::unique_ptr<App> (*make)(); };`
  - `const std::vector<CatalogEntry>& sshos::catalog();` et `const CatalogEntry* sshos::catalog_find(std::string_view id);`
  - `class sshos::Bloc : public App`, plus `int resize_count() const` et `int click_count() const` pour les tests.

- [ ] **Step 1: Écrire le test qui échoue**

Créer `tests/test_app.cpp` :

```cpp
#include <string>

#include "app/app.hpp"
#include "app/catalog.hpp"
#include "apps/bloc.hpp"
#include "harness.hpp"
#include "render/surface.hpp"

using sshos::App;
using sshos::Bloc;
using sshos::CloseCheck;
using sshos::Host;
using sshos::KeyEvent;
using sshos::MouseEvent;
using sshos::Pos;
using sshos::Rect;
using sshos::Size;
using sshos::Style;
using sshos::Surface;
using sshos::View;

namespace {

// Double d'hôte : enregistre ce qu'une application demande, sans rien
// exécuter. Suffit à tout le contrat sauf watch/unwatch, exercés à la
// tâche 8 avec un vrai registrar.
struct FakeHost : Host {
  std::string title;
  int close_requests = 0;
  int invalidations = 0;
  uint64_t next_token = 100;

  void set_title(std::string t) override { title = std::move(t); }
  void request_close() override { ++close_requests; }
  void invalidate() override { ++invalidations; }
  uint64_t watch(int, uint32_t) override { return next_token++; }
  void unwatch(uint64_t) override {}
};

std::string row_of(const Surface& s, int y) { return s.text_row(y); }

}  // namespace

TEST(bloc_announces_its_size_after_a_resize) {
  Bloc app;
  FakeHost host;
  app.attach(host);
  app.on_resize(Size{20, 6});

  Surface s(20, 6);
  View v = s.root();
  app.render(v);
  CHECK(row_of(s, 0).find("taille: 20x6") != std::string::npos);
}

// L'observable qui remplace le compteur du bouchon de session : les trois
// tests du round EPOLLHUP comptent des clics et vérifient qu'ils survivent
// au rattachement (voir la section « migration des tests » du plan).
TEST(bloc_counts_the_presses_it_receives) {
  Bloc app;
  FakeHost host;
  app.attach(host);
  app.on_resize(Size{20, 6});

  Surface s(20, 6);
  {
    View v = s.root();
    app.render(v);
    CHECK(row_of(s, 1).find("clics: 0") != std::string::npos);
  }

  MouseEvent m;
  m.action = sshos::MouseAction::Press;
  m.x = 3;
  m.y = 2;
  app.on_mouse(m);
  app.on_mouse(m);
  CHECK_EQ(app.click_count(), 2);

  s.clear(Style{});
  View v2 = s.root();
  app.render(v2);
  CHECK(row_of(s, 1).find("clics: 2") != std::string::npos);
}

// Le relevé qui sert de preuve à la tâche 5 : un geste de redimensionnement
// entier ne doit produire qu'UN appel, pas un par pixel parcouru.
TEST(bloc_counts_its_resizes) {
  Bloc app;
  FakeHost host;
  app.attach(host);
  CHECK_EQ(app.resize_count(), 0);
  app.on_resize(Size{20, 6});
  app.on_resize(Size{30, 8});
  CHECK_EQ(app.resize_count(), 2);
}

TEST(bloc_moves_its_cursor_with_the_arrows_and_the_mouse) {
  Bloc app;
  FakeHost host;
  app.attach(host);
  app.on_resize(Size{20, 6});

  Pos p;
  CHECK(app.wants_cursor(p));
  CHECK_EQ(p.x, 0);
  CHECK_EQ(p.y, 0);

  app.on_key(KeyEvent{sshos::Key::Right, 0, 0});
  app.on_key(KeyEvent{sshos::Key::Down, 0, 0});
  CHECK(app.wants_cursor(p));
  CHECK_EQ(p.x, 1);
  CHECK_EQ(p.y, 1);

  MouseEvent m;
  m.action = sshos::MouseAction::Press;
  m.x = 7;
  m.y = 3;
  app.on_mouse(m);
  CHECK(app.wants_cursor(p));
  CHECK_EQ(p.x, 7);
  CHECK_EQ(p.y, 3);
}

// Le curseur reste dans la zone cliente même quand elle rétrécit sous lui :
// sinon la position transmise au client désignerait une cellule qui
// n'existe plus, et le terminal placerait son curseur n'importe où.
TEST(bloc_keeps_its_cursor_inside_a_shrinking_client_area) {
  Bloc app;
  FakeHost host;
  app.attach(host);
  app.on_resize(Size{20, 6});
  app.on_key(KeyEvent{sshos::Key::End, 0, 0});

  app.on_resize(Size{5, 2});
  Pos p;
  CHECK(app.wants_cursor(p));
  CHECK(p.x < 5);
  CHECK(p.y < 2);
}

TEST(bloc_sets_its_own_title_on_attach) {
  Bloc app;
  FakeHost host;
  app.attach(host);
  CHECK_EQ(host.title, std::string("Bloc"));
}

TEST(bloc_becomes_modified_on_the_first_keystroke_and_refuses_to_close) {
  Bloc app;
  FakeHost host;
  app.attach(host);
  app.on_resize(Size{20, 6});

  CloseCheck before = app.can_close();
  CHECK(before.allowed);

  app.on_key(KeyEvent{sshos::Key::Char, U'a', 0});
  CloseCheck after = app.can_close();
  CHECK(!after.allowed);
  CHECK(after.question.find("Bloc") != std::string::npos);
  CHECK(host.title.find('*') != std::string::npos);
}

// Un simple déplacement du curseur ne « modifie » rien : sans cette
// distinction, ouvrir une fenêtre et appuyer une fois sur une flèche
// suffirait à déclencher un dialogue de confirmation à la fermeture.
TEST(bloc_is_not_modified_by_a_mere_cursor_move) {
  Bloc app;
  FakeHost host;
  app.attach(host);
  app.on_resize(Size{20, 6});
  app.on_key(KeyEvent{sshos::Key::Right, 0, 0});
  CHECK(app.can_close().allowed);
}

TEST(bloc_declares_a_minimum_size) {
  Bloc app;
  CHECK_EQ(app.min_size().w, 14);
  CHECK_EQ(app.min_size().h, 3);
}

TEST(catalog_lists_bloc_and_can_build_it) {
  const auto& entries = sshos::catalog();
  CHECK(!entries.empty());
  const auto* e = sshos::catalog_find("bloc");
  REQUIRE(e != nullptr);
  CHECK_EQ(e->label, std::string("Bloc"));
  std::unique_ptr<App> made = e->make();
  CHECK(made != nullptr);
  CHECK_EQ(made->min_size().w, 14);
}

TEST(catalog_returns_null_for_an_unknown_identifier) {
  CHECK(sshos::catalog_find("il-n-y-a-personne") == nullptr);
}

// Le contrat par défaut doit être utilisable tel quel : une application qui
// n'implémente que render() doit compiler et se comporter raisonnablement.
TEST(app_defaults_are_usable_without_overriding_anything) {
  struct Minimal : App {
    void render(View) override {}
  };
  Minimal m;
  Pos p{9, 9};
  CHECK(!m.wants_cursor(p));
  CHECK(m.can_close().allowed);
  CHECK(m.min_size().w > 0);
  CHECK(m.on_io(0, 0) == sshos::IoStatus::Ok);
}
```

- [ ] **Step 2: Lancer pour vérifier que ça échoue**

```bash
cmake --build build-release -j"$(nproc)"
```

Attendu : ÉCHEC, `app/app.hpp: No such file or directory`.

- [ ] **Step 3: Écrire `src/app/app.hpp`**

```cpp
#pragma once

#include <cstdint>
#include <string>
#include <utility>

#include "input/events.hpp"
#include "render/cell.hpp"
#include "render/surface.hpp"

namespace sshos {

// Réponse d'une application à « puis-je te fermer ? ». Le refus porte sa
// propre question : c'est l'application qui sait ce qu'elle risque de
// perdre, pas le gestionnaire de fenêtres.
struct CloseCheck {
  bool allowed = true;
  std::string question;

  static CloseCheck allow() { return {}; }
  static CloseCheck ask(std::string q) { return {false, std::move(q)}; }
};

// Ce qu'une application répond après avoir traité un événement sur l'un de
// ses descripteurs. `Closed` veut dire « cette source est morte » : l'hôte
// retire la surveillance et l'application décide ensuite si elle veut
// vivre sans.
enum class IoStatus { Ok, Closed };

// Ce qu'une application a le droit de demander au bureau. Elle ne reçoit
// jamais l'epoll ni son propre numéro de fenêtre : watch() rend un jeton
// opaque, que on_io() lui rendra tel quel.
class Host {
 public:
  virtual ~Host() = default;
  virtual void set_title(std::string title) = 0;
  virtual void request_close() = 0;
  virtual void invalidate() = 0;
  virtual uint64_t watch(int fd, uint32_t events) = 0;
  virtual void unwatch(uint64_t token) = 0;
};

// Le contrat applicatif du projet, valable jusqu'au jalon 6. Tout est
// virtuel avec un défaut utilisable : une application qui ne sait que
// dessiner n'a qu'une méthode à écrire.
class App {
 public:
  virtual ~App() = default;

  // Appelée une fois, à l'ouverture de la fenêtre. La référence reste
  // valable tant que l'application vit.
  virtual void attach(Host& host) { (void)host; }

  // La View est déjà clippée sur la zone cliente : écrire en dehors est
  // ignoré, pas une erreur. C'est ce qui rend structurellement impossible
  // qu'une application peigne par-dessus la barre des tâches.
  virtual void render(View v) = 0;

  virtual void on_key(const KeyEvent& k) { (void)k; }

  // Coordonnées LOCALES à la zone cliente, jamais celles de l'écran.
  virtual void on_mouse(const MouseEvent& m) { (void)m; }

  virtual void on_resize(Size s) { (void)s; }

  virtual IoStatus on_io(uint64_t token, uint32_t events) {
    (void)token;
    (void)events;
    return IoStatus::Ok;
  }

  // `out` en coordonnées locales. Rend false si l'application ne veut pas
  // de curseur visible.
  virtual bool wants_cursor(Pos& out) const {
    (void)out;
    return false;
  }

  // Porte sur la ZONE CLIENTE, pas sur le cadre. Le gestionnaire de
  // fenêtres y ajoute les décorations (voir frame_min, tâche 3).
  virtual Size min_size() const { return {10, 3}; }

  virtual CloseCheck can_close() const { return CloseCheck::allow(); }
};

}  // namespace sshos
```

- [ ] **Step 4: Écrire `src/apps/bloc.hpp` et `src/apps/bloc.cpp`**

`src/apps/bloc.hpp` :

```cpp
#pragma once

#include "app/app.hpp"

namespace sshos {

// Application factice sans descripteur. Aucune utilité pour
// l'utilisateur : son rôle est d'exercer chaque méthode du contrat pour
// que les tests aient prise dessus. Elle reste au catalogue après le
// jalon 3 -- c'est le seul moyen de garder ces chemins vivants une fois
// que de vraies applications existeront.
class Bloc : public App {
 public:
  void attach(Host& host) override;
  void render(View v) override;
  void on_key(const KeyEvent& k) override;
  void on_mouse(const MouseEvent& m) override;
  void on_resize(Size s) override;
  bool wants_cursor(Pos& out) const override;
  Size min_size() const override { return {14, 3}; }
  CloseCheck can_close() const override;

  // Relevés pour les tests. Le compteur de redimensionnements est la preuve
  // qu'un geste entier n'en produit qu'un seul (tâche 5).
  int resize_count() const { return resizes_; }
  int click_count() const { return clicks_; }

 private:
  void clamp_cursor();

  Host* host_ = nullptr;
  Size size_{0, 0};
  Pos cursor_{0, 0};
  int resizes_ = 0;
  int clicks_ = 0;
  bool modified_ = false;
};

}  // namespace sshos
```

`src/apps/bloc.cpp` :

```cpp
#include "apps/bloc.hpp"

#include <algorithm>
#include <string>

namespace sshos {

void Bloc::attach(Host& host) {
  host_ = &host;
  host_->set_title("Bloc");
}

void Bloc::clamp_cursor() {
  // La zone cliente peut rétrécir sous le curseur. Une position hors zone
  // serait transmise telle quelle au client, qui placerait son curseur
  // matériel sur une cellule qui n'existe plus.
  cursor_.x = std::max(0, std::min(cursor_.x, size_.w - 1));
  cursor_.y = std::max(0, std::min(cursor_.y, size_.h - 1));
}

void Bloc::on_resize(Size s) {
  ++resizes_;
  size_ = s;
  clamp_cursor();
}

void Bloc::on_key(const KeyEvent& k) {
  switch (k.key) {
    case Key::Left:
      --cursor_.x;
      break;
    case Key::Right:
      ++cursor_.x;
      break;
    case Key::Up:
      --cursor_.y;
      break;
    case Key::Down:
      ++cursor_.y;
      break;
    case Key::Home:
      cursor_.x = 0;
      break;
    case Key::End:
      cursor_.x = size_.w - 1;
      break;
    default:
      // Toute autre touche est une « saisie » : c'est elle qui rend le
      // document modifié, pas un simple déplacement du curseur. Sans cette
      // distinction, ouvrir une fenêtre et appuyer sur une flèche
      // suffirait à déclencher un dialogue de confirmation à la fermeture.
      if (!modified_) {
        modified_ = true;
        if (host_ != nullptr) host_->set_title("Bloc *");
      }
      break;
  }
  clamp_cursor();
}

void Bloc::on_mouse(const MouseEvent& m) {
  if (m.action != MouseAction::Press) return;
  ++clicks_;
  cursor_.x = m.x;
  cursor_.y = m.y;
  clamp_cursor();
}

bool Bloc::wants_cursor(Pos& out) const {
  out = cursor_;
  return true;
}

CloseCheck Bloc::can_close() const {
  if (!modified_) return CloseCheck::allow();
  return CloseCheck::ask("Bloc a des modifications non enregistrees.");
}

void Bloc::render(View v) {
  Style st;
  st.fg = Color::indexed(7);
  st.bg = Color::indexed(0);
  v.fill(Rect{0, 0, v.w(), v.h()}, st);

  v.text(0, 0, "taille: " + std::to_string(size_.w) + "x" + std::to_string(size_.h), st);
  v.text(0, 1, "clics: " + std::to_string(clicks_), st);
  v.text(0, 2, "resize: " + std::to_string(resizes_), st);

  Style cur = st;
  cur.attrs |= attr::Reverse;
  v.put(cursor_.x, cursor_.y, U'\u2588', cur);
}

}  // namespace sshos
```

- [ ] **Step 5: Écrire `src/app/catalog.hpp` et `src/app/catalog.cpp`**

`src/app/catalog.hpp` :

```cpp
#pragma once

#include <memory>
#include <string>
#include <string_view>
#include <vector>

#include "app/app.hpp"

namespace sshos {

// Une entrée du menu de lancement. `make` est un pointeur de fonction, pas
// un std::function : le catalogue est une table statique, il n'a rien à
// capturer.
struct CatalogEntry {
  std::string id;
  std::string label;
  std::unique_ptr<App> (*make)();
};

const std::vector<CatalogEntry>& catalog();
const CatalogEntry* catalog_find(std::string_view id);

}  // namespace sshos
```

`src/app/catalog.cpp` :

```cpp
#include "app/catalog.hpp"

#include "apps/bloc.hpp"

namespace sshos {
namespace {

std::unique_ptr<App> make_bloc() { return std::make_unique<Bloc>(); }

}  // namespace

const std::vector<CatalogEntry>& catalog() {
  static const std::vector<CatalogEntry> entries = {
      {"bloc", "Bloc", &make_bloc},
  };
  return entries;
}

const CatalogEntry* catalog_find(std::string_view id) {
  for (const auto& e : catalog()) {
    if (e.id == id) return &e;
  }
  return nullptr;
}

}  // namespace sshos
```

`src/app/catalog.cpp` inclut `apps/bloc.hpp`, ce qui fait dépendre `app/` de `apps/`. C'est la seule entorse à la règle de dépendance et elle est délibérée : le catalogue est par définition la liste de ce qui existe. Le contrat lui-même (`app/app.hpp`) reste, lui, totalement ignorant de ses implémentations.

- [ ] **Step 6: Lancer les tests**

```bash
cmake --build build-release -j"$(nproc)" && ./build-release/sshos_tests
```

Attendu : **213 cas, 0 en echec**.

- [ ] **Step 7: Passer les sanitizers**

```bash
cmake --build build-dbg -j"$(nproc)" && \
  UBSAN_OPTIONS=halt_on_error=1:print_stacktrace=1 ./build-dbg/sshos_tests
```

- [ ] **Step 8: Commit**

```bash
git add src/app src/apps tests/test_app.cpp
git commit -m "feat(app): contrat App/Host, catalogue et application factice Bloc"
```

---

### Task 3: `Window`, `frame_min`, décorations

La géométrie d'une fenêtre et son dessin, sans démon, sans souris, sans pile. Deux fonctions portent tout le reste du jalon : `decor_metrics()` dit **où** sont la barre de titre, les boutons et la zone cliente ; `draw_decor()` les peint. La tâche 5 écrira `hit_window()`, qui doit être l'inverse exact de `decor_metrics()` — c'est pour cela que la métrique est une fonction pure séparée du dessin, et non des calculs noyés dans une boucle de rendu.

Deux points à ne pas improviser :

**L'ordre de déclaration des membres de `Window` est significatif.** `host` est déclaré **avant** `app` : les membres sont détruits dans l'ordre inverse de leur déclaration, donc `app` meurt en premier et peut encore appeler `host->unwatch()` dans son destructeur. L'ordre inverse ferait déréférencer un hôte déjà détruit par Battement à la tâche 8.

**Les glyphes des décorations sont East Asian Ambiguous** (`─ │ ┌ ┐ └ ┘ □ × ◢`). Leur largeur dépend d'une politique globale, `set_ambiguous_wide()` (`src/render/width.cpp:50`), que le démon fixe à `false` à chaque attache (`src/daemon/daemon.cpp:475`) : ils occupent donc **une** cellule. Toute la géométrie des boutons en dépend. Si cette politique changeait un jour, le test de propriété de la tâche 5 (hit-test = inverse exact de la composition) le signalerait immédiatement — c'est lui la garde, pas un commentaire.

**Files:**
- Create: `src/wm/window.hpp`, `src/wm/window.cpp`
- Create: `src/wm/decor.hpp`, `src/wm/decor.cpp`
- Create: `tests/test_wm.cpp`

**Interfaces:**
- Consumes: `App`, `Host` (tâche 2) ; `Bloc` (tâche 2) ; `Theme`, `Border`, `View::box` (tâche 1) ; `Rect`, `Size`, `Style`, `View` (jalon 1).
- Produces:
  - `using sshos::WindowId = uint32_t;` — monotone, jamais réutilisé, commence à 1.
  - `enum class sshos::WinMode { Normal, Minimized, Maximized, Fullscreen };`
  - `struct sshos::Window` : `id`, `title`, `user_rect`, `display_rect`, `mode`, `before_fullscreen`, `host` (`std::unique_ptr<Host>`), `app` (`std::unique_ptr<App>`), `sent_size`, `close_requested`.
  - `Rect sshos::client_rect(const Rect& frame)` — `{x+1, y+1, w-2, h-2}`, jamais de dimension négative.
  - `Size sshos::frame_min(const App& app)` — `{max(min_size().w + 2, 16), max(min_size().h + 2, 5)}`.
  - `Rect sshos::clamp_to(Rect frame, const Rect& work, Size min)` — ramène dans la zone de travail ; le plancher l'emporte sur la zone si elle est plus petite.
  - `struct sshos::DecorMetrics { Rect title_bar, title_text, buttons; int button_count; Rect client; };`
  - `DecorMetrics sshos::decor_metrics(const Rect& frame)`.
  - `void sshos::draw_decor(View v, const Window& w, bool focused, const Theme& th, Border b)` — `v` est la vue **racine** de l'écran ; les coordonnées sont absolues.

- [ ] **Step 1: Écrire le test qui échoue**

Créer `tests/test_wm.cpp` :

```cpp
#include <memory>
#include <string>

#include "app/app.hpp"
#include "apps/bloc.hpp"
#include "harness.hpp"
#include "render/surface.hpp"
#include "render/theme.hpp"
#include "wm/decor.hpp"
#include "wm/window.hpp"

using sshos::App;
using sshos::Bloc;
using sshos::Border;
using sshos::DecorMetrics;
using sshos::Rect;
using sshos::Size;
using sshos::Style;
using sshos::Surface;
using sshos::Theme;
using sshos::View;
using sshos::Window;
using sshos::WindowId;
using sshos::clamp_to;
using sshos::client_rect;
using sshos::decor_metrics;
using sshos::draw_decor;
using sshos::frame_min;

namespace {

Window make_window(WindowId id, std::string title, Rect r) {
  Window w;
  w.id = id;
  w.title = std::move(title);
  w.user_rect = r;
  w.display_rect = r;
  w.app = std::make_unique<Bloc>();
  return w;
}

}  // namespace

TEST(client_rect_removes_the_decorations) {
  CHECK(client_rect(Rect{10, 4, 20, 8}) == (Rect{11, 5, 18, 6}));
}

TEST(client_rect_never_reports_a_negative_size) {
  const Rect c = client_rect(Rect{0, 0, 1, 1});
  CHECK(c.w >= 0);
  CHECK(c.h >= 0);
}

// min_size() porte sur la zone CLIENTE ; le plancher 16x5 porte sur le
// CADRE. Confondre les deux donnerait des fenêtres de 14 colonnes dont la
// zone utile n'en ferait que 12.
TEST(frame_min_adds_the_decorations_and_honours_the_floor) {
  Bloc small;
  CHECK(frame_min(small) == (Size{16, 5}));

  struct Big : App {
    void render(View) override {}
    Size min_size() const override { return {30, 20}; }
  };
  Big big;
  CHECK(frame_min(big) == (Size{32, 22}));
}

TEST(clamp_to_pushes_a_window_back_inside_the_work_area) {
  const Rect work{0, 0, 80, 23};
  CHECK(clamp_to(Rect{70, 20, 44, 14}, work, Size{16, 5}) == (Rect{36, 9, 44, 14}));
}

// Quand la zone de travail est plus petite que le plancher, le plancher
// gagne : une fenêtre qui déborde reste utilisable, une fenêtre écrasée à
// 4x2 ne l'est pas.
TEST(clamp_to_lets_the_floor_win_over_a_tiny_work_area) {
  const Rect r = clamp_to(Rect{0, 0, 100, 100}, Rect{0, 0, 20, 6}, Size{16, 5});
  CHECK_EQ(r.w, 20);
  CHECK_EQ(r.h, 6);

  const Rect tiny = clamp_to(Rect{0, 0, 100, 100}, Rect{0, 0, 4, 2}, Size{16, 5});
  CHECK_EQ(tiny.w, 16);
  CHECK_EQ(tiny.h, 5);
}

TEST(decor_metrics_places_three_buttons_flush_right) {
  const DecorMetrics m = decor_metrics(Rect{2, 1, 30, 10});
  CHECK_EQ(m.button_count, 3);
  CHECK(m.title_bar == (Rect{2, 1, 30, 1}));
  CHECK_EQ(m.buttons.x + m.buttons.w, 2 + 30 - 1);
  CHECK_EQ(m.buttons.w, 9);
  CHECK(m.client == (Rect{3, 2, 28, 8}));
}

// « Les boutons à toutes les largeurs » de la spec, §12.1 famille 1. Les
// seuils tombent de la même règle : n boutons demandent 7 + 3n colonnes
// (2 de marge, 3 de titre lisible, 1 de marge à droite, 1 de séparation).
TEST(decor_metrics_elides_buttons_in_order_as_the_frame_narrows) {
  int previous = 3;
  for (int w = 40; w >= 4; --w) {
    const DecorMetrics m = decor_metrics(Rect{0, 0, w, 5});
    CHECK(m.button_count <= previous);
    CHECK(m.button_count >= 0);
    CHECK(m.button_count <= 3);
    if (m.button_count > 0) {
      CHECK_EQ(m.buttons.x + m.buttons.w, w - 1);
      CHECK(m.title_text.x + m.title_text.w <= m.buttons.x);
    }
    CHECK(m.title_text.w >= 0);
    previous = m.button_count;
  }
  CHECK_EQ(decor_metrics(Rect{0, 0, 16, 5}).button_count, 3);
  CHECK_EQ(decor_metrics(Rect{0, 0, 15, 5}).button_count, 2);
  CHECK_EQ(decor_metrics(Rect{0, 0, 13, 5}).button_count, 2);
  CHECK_EQ(decor_metrics(Rect{0, 0, 12, 5}).button_count, 1);
  CHECK_EQ(decor_metrics(Rect{0, 0, 10, 5}).button_count, 1);
  CHECK_EQ(decor_metrics(Rect{0, 0, 9, 5}).button_count, 0);
}

TEST(draw_decor_paints_the_title_bar_across_the_full_width) {
  Surface s(30, 10);
  Window w = make_window(1, "Bloc", Rect{2, 1, 20, 7});
  View v = s.root();
  const Theme th = Theme::mono16();
  draw_decor(v, w, true, th, Border::Ascii);

  CHECK(s.text_row(1).find("Bloc") != std::string::npos);
  CHECK_EQ(s.at(2, 1).bg, th.title_focus_bg);
  CHECK_EQ(s.at(21, 1).bg, th.title_focus_bg);
  CHECK(!(s.at(1, 1).bg == th.title_focus_bg));  // rien en dehors du cadre
}

TEST(draw_decor_distinguishes_a_focused_window_from_a_blurred_one) {
  Surface a(30, 10);
  Surface b(30, 10);
  Window w1 = make_window(1, "Bloc", Rect{0, 0, 20, 7});
  Window w2 = make_window(2, "Bloc", Rect{0, 0, 20, 7});
  const Theme th = Theme::mono16();
  View va = a.root();
  View vb = b.root();
  draw_decor(va, w1, true, th, Border::Ascii);
  draw_decor(vb, w2, false, th, Border::Ascii);

  CHECK(!(a.at(0, 3).fg == b.at(0, 3).fg));  // bordure gauche
  CHECK(!(a.at(0, 0).bg == b.at(0, 0).bg));  // barre de titre
}

TEST(draw_decor_puts_a_resize_handle_in_the_bottom_right_corner) {
  Surface s(30, 10);
  Window w = make_window(1, "Bloc", Rect{2, 1, 20, 7});
  View v = s.root();
  draw_decor(v, w, true, Theme::mono16(), Border::Unicode);
  CHECK_EQ(s.at(21, 7).ch, U'\u25e2');
}

TEST(draw_decor_never_lets_a_long_title_reach_the_buttons) {
  Surface s(30, 10);
  Window w = make_window(1, std::string(50, 'X'), Rect{0, 0, 20, 7});
  View v = s.root();
  draw_decor(v, w, true, Theme::mono16(), Border::Ascii);

  const DecorMetrics m = decor_metrics(w.display_rect);
  REQUIRE(m.button_count == 3);
  for (int x = m.buttons.x; x < m.buttons.x + m.buttons.w; ++x) {
    CHECK(s.at(x, 0).ch != U'X');
  }
  CHECK_EQ(s.at(m.buttons.x, 0).ch, U'[');
}

TEST(draw_decor_falls_back_to_ascii) {
  Surface s(30, 10);
  Window w = make_window(1, "Bloc", Rect{0, 0, 20, 7});
  View v = s.root();
  draw_decor(v, w, true, Theme::mono16(), Border::Ascii);
  CHECK_EQ(s.at(0, 6).ch, U'+');
  CHECK_EQ(s.at(1, 6).ch, U'-');
  CHECK_EQ(s.at(0, 3).ch, U'|');
  CHECK_EQ(s.at(19, 6).ch, U'#');
}

// L'ordre de déclaration des membres de Window n'est pas cosmétique : app
// doit mourir AVANT host, sans quoi une application qui appelle
// host->unwatch() dans son destructeur (Battement, tâche 8) déréférencerait
// un hôte déjà détruit. On le vérifie ici, tant que c'est encore bon
// marché.
TEST(window_destroys_its_app_before_its_host) {
  static int order = 0;
  static int host_died = 0;
  static int app_died = 0;
  order = 0;
  host_died = 0;
  app_died = 0;

  struct NoisyHost : sshos::Host {
    ~NoisyHost() override { host_died = ++order; }
    void set_title(std::string) override {}
    void request_close() override {}
    void invalidate() override {}
    uint64_t watch(int, uint32_t) override { return 0; }
    void unwatch(uint64_t) override {}
  };
  struct NoisyApp : App {
    ~NoisyApp() override { app_died = ++order; }
    void render(View) override {}
  };

  {
    Window w;
    w.host = std::make_unique<NoisyHost>();
    w.app = std::make_unique<NoisyApp>();
  }
  CHECK_EQ(app_died, 1);
  CHECK_EQ(host_died, 2);
}
```

- [ ] **Step 2: Lancer pour vérifier que ça échoue**

```bash
cmake --build build-release -j"$(nproc)"
```

Attendu : ÉCHEC, `wm/window.hpp: No such file or directory`.

- [ ] **Step 3: Écrire `src/wm/window.hpp`**

```cpp
#pragma once

#include <cstdint>
#include <memory>
#include <string>

#include "app/app.hpp"
#include "render/cell.hpp"

namespace sshos {

// Monotone, jamais réutilisé. Un identifiant recyclé ferait qu'une action
// différée (une fermeture, un événement de descripteur en retard) frappe
// une fenêtre qui n'est pas celle qu'on visait.
using WindowId = uint32_t;

enum class WinMode { Normal, Minimized, Maximized, Fullscreen };

struct Window {
  WindowId id = 0;
  std::string title;

  // user_rect est la géométrie VOULUE, en mode Normal, jamais écrasée par
  // une contrainte d'affichage. display_rect en est la projection dans la
  // zone de travail du moment. C'est cette séparation qui rend le
  // redimensionnement du terminal réversible : rétrécir puis rétablir
  // rend exactement la disposition d'origine.
  Rect user_rect{};
  Rect display_rect{};

  WinMode mode = WinMode::Normal;
  WinMode before_fullscreen = WinMode::Normal;

  // ORDRE SIGNIFICATIF : les membres sont détruits dans l'ordre inverse de
  // leur déclaration. `app` doit mourir avant `host`, sinon une
  // application qui appelle host->unwatch() dans son destructeur (voir
  // Battement) déréférence un hôte déjà détruit.
  std::unique_ptr<Host> host;
  std::unique_ptr<App> app;

  // Dernière taille annoncée à l'application. Sert à n'appeler on_resize()
  // que lorsqu'elle change réellement.
  Size sent_size{-1, -1};

  bool close_requested = false;
};

Rect client_rect(const Rect& frame);
Size frame_min(const App& app);
Rect clamp_to(Rect frame, const Rect& work, Size min);

}  // namespace sshos
```

- [ ] **Step 4: Écrire `src/wm/window.cpp`**

```cpp
#include "wm/window.hpp"

#include <algorithm>

namespace sshos {

Rect client_rect(const Rect& frame) {
  Rect c{frame.x + 1, frame.y + 1, frame.w - 2, frame.h - 2};
  c.w = std::max(0, c.w);
  c.h = std::max(0, c.h);
  return c;
}

Size frame_min(const App& app) {
  const Size m = app.min_size();
  // min_size() porte sur la zone cliente, le plancher du projet sur le
  // cadre : on ajoute les décorations avant de comparer.
  return Size{std::max(m.w + 2, 16), std::max(m.h + 2, 5)};
}

Rect clamp_to(Rect frame, const Rect& work, Size min) {
  frame.w = std::max(min.w, std::min(frame.w, work.w));
  frame.h = std::max(min.h, std::min(frame.h, work.h));
  // Si le plancher dépasse la zone de travail, les deux max() ci-dessous
  // collent la fenêtre en haut à gauche et la laissent déborder. C'est
  // voulu : une fenêtre qui déborde reste utilisable, une fenêtre écrasée
  // sous son minimum ne l'est pas.
  frame.x = std::max(work.x, std::min(frame.x, work.x + work.w - frame.w));
  frame.y = std::max(work.y, std::min(frame.y, work.y + work.h - frame.h));
  return frame;
}

}  // namespace sshos
```

- [ ] **Step 5: Écrire `src/wm/decor.hpp`**

```cpp
#pragma once

#include "render/cell.hpp"
#include "render/surface.hpp"
#include "render/theme.hpp"
#include "wm/window.hpp"

namespace sshos {

// Toute la géométrie des décorations, en coordonnées ABSOLUES. Fonction
// pure, séparée du dessin : hit_window() (tâche 5) doit en être l'inverse
// exact, et deux calculs enfouis dans deux boucles de rendu divergeraient
// à la première retouche.
struct DecorMetrics {
  Rect title_bar;   // ligne du haut, toute la largeur du cadre
  Rect title_text;  // là où le titre a le droit de s'écrire
  Rect buttons;     // w == 0 quand ils sont tous élidés
  int button_count = 0;
  Rect client;
};

DecorMetrics decor_metrics(const Rect& frame);

// `v` est la vue RACINE de l'écran : draw_decor peint en coordonnées
// absolues, à partir de w.display_rect.
void draw_decor(View v, const Window& w, bool focused, const Theme& th, Border b);

}  // namespace sshos
```

- [ ] **Step 6: Écrire `src/wm/decor.cpp`**

```cpp
#include "wm/decor.hpp"

#include <algorithm>

namespace sshos {
namespace {

// Trois cellules par bouton. Le titre a besoin d'au moins 3 colonnes pour
// rester lisible, avec 2 colonnes de marge à gauche, 1 de séparation avant
// les boutons et 1 de marge à droite : n boutons demandent donc 7 + 3n
// colonnes de cadre.
constexpr int kButtonWidth = 3;
constexpr int kTitleMargin = 2;

}  // namespace

DecorMetrics decor_metrics(const Rect& frame) {
  DecorMetrics m;
  m.title_bar = Rect{frame.x, frame.y, frame.w, 1};
  m.client = client_rect(frame);

  int n = 0;
  for (int candidate = 3; candidate >= 1; --candidate) {
    if (frame.w >= 7 + kButtonWidth * candidate) {
      n = candidate;
      break;
    }
  }
  m.button_count = n;

  const int bx = frame.x + frame.w - 1 - kButtonWidth * n;
  m.buttons = Rect{bx, frame.y, kButtonWidth * n, 1};

  const int title_x = frame.x + kTitleMargin;
  const int title_end = (n > 0) ? bx - 1 : frame.x + frame.w - 1;
  m.title_text = Rect{title_x, frame.y, std::max(0, title_end - title_x), 1};
  return m;
}

void draw_decor(View v, const Window& w, bool focused, const Theme& th, Border b) {
  const Rect f = w.display_rect;
  if (f.w <= 0 || f.h <= 0) return;

  const bool uni = (b == Border::Unicode);
  const DecorMetrics m = decor_metrics(f);

  // Une fenêtre est opaque : on remplit tout le cadre avant de dessiner,
  // sinon le bureau transparaîtrait partout où l'application ne peint pas.
  Style frame_st;
  frame_st.bg = th.panel_bg;
  frame_st.fg = focused ? th.border_focus : th.border_blur;
  v.fill(f, frame_st);
  v.box(f, b, frame_st);

  Style title_st;
  title_st.bg = focused ? th.title_focus_bg : th.title_blur_bg;
  title_st.fg = focused ? th.title_focus_fg : th.title_blur_fg;
  v.fill(m.title_bar, title_st);

  // Le titre s'écrit dans SA vue : le clip fait la troncature, il n'y a pas
  // de comptage de caractères à tenir juste.
  if (m.title_text.w > 0) {
    View tv = v.sub(m.title_text);
    tv.text(0, 0, w.title, title_st);
  }

  // Élidés dans l'ordre inverse de leur utilité : réduire part en premier,
  // fermer reste en dernier.
  const char* labels[3] = {"[_]", uni ? "[\xe2\x96\xa1]" : "[o]",
                           uni ? "[\xc3\x97]" : "[x]"};
  for (int i = 0; i < m.button_count; ++i) {
    const int which = 3 - m.button_count + i;
    View bv = v.sub(Rect{m.buttons.x + kButtonWidth * i, m.buttons.y, kButtonWidth, 1});
    bv.text(0, 0, labels[which], title_st);
  }

  // La poignée occupe le coin bas-droit, qui n'existe que si le cadre a au
  // moins deux lignes -- sur une seule, la barre de titre l'a déjà pris.
  if (f.h >= 2) {
    Style grip = frame_st;
    grip.fg = th.accent;
    v.put(f.x + f.w - 1, f.y + f.h - 1, uni ? U'\u25e2' : U'#', grip);
  }
}

}  // namespace sshos
```

- [ ] **Step 7: Lancer les tests**

```bash
cmake --build build-release -j"$(nproc)" && ./build-release/sshos_tests
```

Attendu : **226 cas, 0 en echec**.

- [ ] **Step 8: Passer les sanitizers**

```bash
cmake --build build-dbg -j"$(nproc)" && \
  UBSAN_OPTIONS=halt_on_error=1:print_stacktrace=1 ./build-dbg/sshos_tests
```

- [ ] **Step 9: Commit**

```bash
git add src/wm tests/test_wm.cpp
git commit -m "feat(wm): geometrie de fenetre, metrique et dessin des decorations"
```

---

### Task 4: `Session` recomposée — le squelette vertical devient visible

La tâche qui rend le jalon visible : à la fin, une connexion SSH montre une vraie fenêtre décorée, avec une vraie application dedans, au-dessus d'un panneau. Le bouchon de 123 lignes disparaît.

C'est aussi la tâche qui touche douze tests existants. **Lire la section « La migration des tests du bouchon » plus haut avant de commencer** : elle nomme les quatre aiguilles à changer et explique pourquoi les huit autres tests survivent verbatim.

Le routage de la souris est ici volontairement minimal — un clic dans la zone cliente de la fenêtre part à l'application, tout le reste est ignoré. La tâche 5 le remplace par le vrai hit-test. Ce n'est pas une dette : c'est ce qu'il faut pour que les trois tests du round EPOLLHUP restent verts à chaque commit intermédiaire, ce que « pas de routage du tout jusqu'à la tâche 5 » ne permettrait pas.

**Files:**
- Create: `src/daemon/host.hpp`, `src/daemon/host.cpp`
- Rewrite: `src/daemon/session.hpp`, `src/daemon/session.cpp`
- Modify: `src/daemon/daemon.cpp` (un seul endroit, autour de la ligne 473)
- Modify: `tests/test_session.cpp` (quatre aiguilles, plus les nouveaux cas)

**Interfaces:**
- Consumes: `Window`, `client_rect`, `frame_min`, `clamp_to` (tâche 3) ; `draw_decor` (tâche 3) ; `Bloc` (tâche 2) ; `Theme`, `Border` (tâche 1) ; `OutputProfile` (jalon 1).
- Produces:
  - `class sshos::HostImpl : public Host` — construit avec `Window&`, écrit le titre dans la fenêtre, note la demande de fermeture.
  - `class sshos::Session` : `Session(Platform&, int cols, int rows)`, `void set_output(const OutputProfile&)`, `void resize(int, int)`, `void on_input(const InputEvent&)`, `void render(Surface&)`, `bool wants_quit() const`.

- [ ] **Step 1: Écrire les tests qui échouent**

Dans `tests/test_session.cpp`, **remplacer** `session_draws_a_bordered_box_with_its_title` (ligne 212) par :

```cpp
TEST(session_draws_a_decorated_window_with_its_title) {
  FakePlatform plat;
  Session sess(plat, 40, 12);
  Surface s(40, 12);
  sess.render(s);

  bool found_title = false;
  for (int y = 0; y < 11; ++y) {
    if (s.text_row(y).find("Bloc") != std::string::npos) found_title = true;
  }
  CHECK(found_title);
}
```

Dans `session_geometry_follows_the_surface_not_the_constructor_arguments` (lignes 306 et 316), remplacer les deux `"ssh_os 2.0"` par `"Bloc"`. **Ne rien changer d'autre dans ce test** : ses assertions sur `"ssh_os"` en dernière ligne et son absence l'avant-dernière restent exactes, le panneau gardant la marque.

Aux lignes 894 et 1323, remplacer `"ssh_os 2.0"` par `"Bloc"` dans les deux appels à `wait_for_frame_containing`.

Puis ajouter à la suite des cas unitaires de `Session` :

```cpp
TEST(session_forwards_a_click_inside_the_client_area_to_the_app) {
  FakePlatform plat;
  Session sess(plat, 80, 24);
  Surface s(80, 24);
  sess.render(s);  // crée la fenêtre

  sshos::MouseEvent m;
  m.action = sshos::MouseAction::Press;
  m.x = 9;
  m.y = 4;
  sess.on_input(sshos::InputEvent{m});

  Surface again(80, 24);
  sess.render(again);
  bool found = false;
  for (int y = 0; y < 23; ++y) {
    if (again.text_row(y).find("clics: 1") != std::string::npos) found = true;
  }
  CHECK(found);
}

// Le pendant du test précédent : un clic sur la décoration ou sur le
// bureau ne doit PAS être livré à l'application. Sans cette garde, une
// application recevrait des coordonnées négatives ou hors de sa surface.
TEST(session_does_not_forward_a_click_outside_the_client_area) {
  FakePlatform plat;
  Session sess(plat, 80, 24);
  Surface s(80, 24);
  sess.render(s);

  sshos::MouseEvent m;
  m.action = sshos::MouseAction::Press;
  m.x = 2;  // colonne gauche du cadre : la bordure, pas la zone cliente
  m.y = 5;
  sess.on_input(sshos::InputEvent{m});

  Surface again(80, 24);
  sess.render(again);
  bool zero = false;
  for (int y = 0; y < 23; ++y) {
    if (again.text_row(y).find("clics: 0") != std::string::npos) zero = true;
  }
  CHECK(zero);
}

// Une frame rendue deux fois de suite à taille constante ne doit annoncer
// qu'UN redimensionnement. C'est le relevé qui servira de preuve au geste
// complet, à la tâche 5.
TEST(session_announces_a_size_change_only_when_it_changes) {
  FakePlatform plat;
  Session sess(plat, 80, 24);
  Surface s(80, 24);
  sess.render(s);
  sess.render(s);
  sess.render(s);

  bool once = false;
  for (int y = 0; y < 23; ++y) {
    if (s.text_row(y).find("resize: 1") != std::string::npos) once = true;
  }
  CHECK(once);
}

TEST(session_uses_ascii_borders_until_the_client_announces_utf8) {
  FakePlatform plat;
  Session sess(plat, 80, 24);

  Surface ascii(80, 24);
  sess.render(ascii);
  CHECK_EQ(ascii.at(2, 14).ch, U'+');  // coin bas-gauche du cadre {2,1,44,14}

  sshos::OutputProfile p;
  p.depth = sshos::ColorDepth::TrueColor;
  p.utf8 = true;
  sess.set_output(p);

  Surface uni(80, 24);
  sess.render(uni);
  CHECK_EQ(uni.at(2, 14).ch, U'\u2514');
}

// La zone de travail s'arrête au-dessus du panneau : une fenêtre ne peut
// pas recouvrir la barre des tâches, quelle que soit sa géométrie voulue.
TEST(session_keeps_every_window_above_the_panel) {
  FakePlatform plat;
  Session sess(plat, 80, 24);
  Surface s(80, 24);
  sess.render(s);
  CHECK(s.text_row(23).find("ssh_os") != std::string::npos);
}
```

- [ ] **Step 2: Lancer pour vérifier que ça échoue**

```bash
cmake --build build-release -j"$(nproc)" && ./build-release/sshos_tests session
```

Attendu : ÉCHEC. `session_draws_a_decorated_window_with_its_title` ne trouve pas `"Bloc"`, `session_forwards_a_click...` ne trouve pas `"clics: 1"`, et `set_output` n'existe pas (erreur de compilation).

- [ ] **Step 3: Écrire `src/daemon/host.hpp` et `src/daemon/host.cpp`**

`src/daemon/host.hpp` :

```cpp
#pragma once

#include <cstdint>
#include <string>

#include "app/app.hpp"
#include "wm/window.hpp"

namespace sshos {

// Le seul objet qui traverse la frontière entre une application et le
// bureau. Il tient une référence sur SA fenêtre, dont l'adresse est stable
// parce que les fenêtres vivent derrière des unique_ptr, jamais dans un
// vector d'objets déplaçables.
class HostImpl : public Host {
 public:
  explicit HostImpl(Window& win) : win_(&win) {}

  void set_title(std::string title) override;
  void request_close() override;
  void invalidate() override;

  // watch/unwatch sont câblés à la tâche 8, avec le FdRegistrar et les clés
  // générationnelles. Ne rien faire est le comportement CORRECT tant
  // qu'aucune application du catalogue ne surveille de descripteur : Bloc
  // n'en a pas, et Battement -- qui en a un -- arrive avec le registrar.
  uint64_t watch(int fd, uint32_t events) override;
  void unwatch(uint64_t token) override;

 private:
  Window* win_;
};

}  // namespace sshos
```

`src/daemon/host.cpp` :

```cpp
#include "daemon/host.hpp"

namespace sshos {

void HostImpl::set_title(std::string title) { win_->title = std::move(title); }

void HostImpl::request_close() { win_->close_requested = true; }

void HostImpl::invalidate() {
  // Tout changement d'état d'une application vient aujourd'hui d'une
  // entrée utilisateur, que le démon marque déjà comme salissante. Le canal
  // session -> démon arrive à la tâche 9, avec l'horloge : c'est le premier
  // composant qui doit réclamer un repaint sans qu'on ait touché à une
  // touche.
}

uint64_t HostImpl::watch(int fd, uint32_t events) {
  (void)fd;
  (void)events;
  return 0;
}

void HostImpl::unwatch(uint64_t token) { (void)token; }

}  // namespace sshos
```

Avant de passer à la suite, **vérifier l'affirmation du commentaire de `invalidate()`** : dans `src/daemon/daemon.cpp`, la branche `EPOLLIN` du client doit marquer la frame sale après avoir passé les événements à la session. Si ce n'est pas le cas, le dire dans le rapport de tâche plutôt que d'ajuster le commentaire.

- [ ] **Step 4: Écrire `src/daemon/session.hpp`**

```cpp
#pragma once

#include <memory>

#include "common/platform.hpp"
#include "input/events.hpp"
#include "render/profile.hpp"
#include "render/surface.hpp"
#include "render/theme.hpp"
#include "wm/window.hpp"

namespace sshos {

class Session {
 public:
  Session(Platform& plat, int cols, int rows);

  // Appelée par le démon à chaque attache, là où il construit déjà le
  // profil pour le Differ. Détermine le thème ET le jeu de bordures : un
  // client sans UTF-8 reçoit des cadres ASCII, pas des points
  // d'interrogation.
  void set_output(const OutputProfile& p);

  void resize(int cols, int rows);
  void on_input(const InputEvent& e);
  void render(Surface& out);
  bool wants_quit() const { return quit_; }

 private:
  Rect work_area(int cols, int rows) const;
  Border border() const;
  void ensure_window(const Rect& work);
  void draw_panel(View& v, int cols, int rows);

  Platform* plat_;
  OutputProfile out_;
  Theme theme_;
  bool quit_ = false;

  // Une seule fenêtre jusqu'à la tâche 6, derrière un unique_ptr : son
  // adresse doit rester stable, HostImpl la référence.
  std::unique_ptr<Window> win_;
  WindowId next_id_ = 1;
};

}  // namespace sshos
```

- [ ] **Step 5: Écrire `src/daemon/session.cpp`**

```cpp
#include "daemon/session.hpp"

#include <cstdio>
#include <ctime>
#include <string>
#include <variant>

#include "apps/bloc.hpp"
#include "daemon/host.hpp"
#include "wm/decor.hpp"

namespace sshos {
namespace {

constexpr int kMinCols = 40;
constexpr int kMinRows = 12;

constexpr char kFullWarning[] = "terminal trop petit - 40x12 minimum";
constexpr char kShortWarning[] = "trop petit";
constexpr int kFullWarningLen = sizeof(kFullWarning) - 1;

// Position et taille de la première fenêtre, déterministes : plusieurs
// tests bout-en-bout cliquent à une coordonnée fixe en comptant sur le fait
// qu'elle tombe dans la zone cliente. La cascade et le placement libre
// arrivent à la tâche 6.
constexpr Rect kFirstWindowRect{2, 1, 44, 14};

// REPRENDRE clock_text() TEL QUEL depuis l'ancien session.cpp, commentaire
// compris. Ses trente lignes d'explication (heure locale, ::tzset(),
// environnement hérité par le démon détaché) sont la trace d'un round
// entier, et deux tests -- dont la preuve de l'heure d'été -- en dépendent.
std::string clock_text(const Platform& plat);

}  // namespace

Session::Session(Platform& plat, int, int) : plat_(&plat) {
  theme_ = Theme::defaults().for_profile(out_);
}

void Session::set_output(const OutputProfile& p) {
  out_ = p;
  theme_ = Theme::defaults().for_profile(p);
}

// Les arguments du constructeur et de resize() restent ignorés : render()
// tire TOUTE sa géométrie de la Surface qu'on lui passe, à chaque appel.
// C'est la propriété A3 du jalon 1, gardée par
// session_geometry_follows_the_surface_not_the_constructor_arguments.
void Session::resize(int, int) {}

Border Session::border() const {
  return out_.utf8 ? Border::Unicode : Border::Ascii;
}

Rect Session::work_area(int cols, int rows) const {
  // Panneau ancré en bas, épaisseur 1. Les quatre bords arrivent à la
  // tâche 9.
  return Rect{0, 0, cols, rows - 1};
}

void Session::ensure_window(const Rect& work) {
  if (win_) return;
  auto w = std::make_unique<Window>();
  w->id = next_id_++;
  w->user_rect = kFirstWindowRect;
  w->app = std::make_unique<Bloc>();
  w->display_rect = clamp_to(w->user_rect, work, frame_min(*w->app));
  // L'hôte est créé APRÈS la fenêtre et AVANT attach() : c'est attach() qui
  // fait poser son titre à l'application.
  w->host = std::make_unique<HostImpl>(*w);
  w->app->attach(*w->host);
  win_ = std::move(w);
}

void Session::draw_panel(View& v, int cols, int rows) {
  Style p;
  p.bg = theme_.panel_bg;
  p.fg = theme_.panel_fg;
  const int py = rows - 1;
  v.fill(Rect{0, py, cols, 1}, p);

  // Le bouton de menu porte la marque du projet. En UTF-8 il gagne son
  // glyphe ; sans UTF-8 le mot seul reste lisible, là où un point
  // d'interrogation ne dirait rien. Il devient cliquable à la tâche 10.
  v.text(1, py, out_.utf8 ? "\xe2\x98\xb0 ssh_os" : "ssh_os", p);

  const std::string t = clock_text(*plat_);
  v.text(cols - static_cast<int>(t.size()) - 1, py, t, p);
}

void Session::on_input(const InputEvent& e) {
  if (const auto* k = std::get_if<KeyEvent>(&e)) {
    if (k->key == Key::Char && k->ch == U'q' && (k->mods & mod::Ctrl) != 0) {
      quit_ = true;
      return;
    }
    if (win_) win_->app->on_key(*k);
    return;
  }
  if (const auto* m = std::get_if<MouseEvent>(&e)) {
    if (!win_) return;
    // Routage volontairement minimal : la tâche 5 le remplace par le vrai
    // hit-test. Il suffit à garder verts les trois tests du round EPOLLHUP
    // à chaque commit intermédiaire.
    const Rect cr = client_rect(win_->display_rect);
    if (!cr.contains(m->x, m->y)) return;
    MouseEvent local = *m;
    local.x = m->x - cr.x;
    local.y = m->y - cr.y;
    win_->app->on_mouse(local);
  }
}

void Session::render(Surface& out) {
  View v = out.root();
  Style desk;
  desk.bg = theme_.desktop_bg;
  v.fill(Rect{0, 0, out.w(), out.h()}, desk);

  if (out.w() < kMinCols || out.h() < kMinRows) {
    // On sort AVANT de toucher à quoi que ce soit : l'état du bureau est
    // préservé intact, et réagrandir le terminal le rend tel quel.
    Style warn;
    warn.fg = theme_.panel_fg;
    if (out.w() >= kFullWarningLen) {
      v.text(0, 0, kFullWarning, warn);
    } else {
      v.text(0, 0, kShortWarning, warn);
    }
    return;
  }

  const Rect work = work_area(out.w(), out.h());
  ensure_window(work);

  Window& w = *win_;
  w.display_rect = clamp_to(w.user_rect, work, frame_min(*w.app));
  const Rect cr = client_rect(w.display_rect);
  const Size cs{cr.w, cr.h};
  if (!(cs == w.sent_size)) {
    w.app->on_resize(cs);
    w.sent_size = cs;
  }

  draw_decor(v, w, true, theme_, border());
  w.app->render(v.sub(cr));

  // Le panneau passe en dernier. clamp_to() garantit déjà qu'aucune fenêtre
  // ne l'atteint ; le dessiner par-dessus coûte une ligne et supprime toute
  // une classe de régressions futures.
  draw_panel(v, out.w(), out.h());
}

}  // namespace sshos
```

- [ ] **Step 6: Câbler le profil dans `daemon.cpp`**

Autour de la ligne 473, la construction du `Differ` appelle `OutputProfile::detect(...)` en argument direct. La hisser dans une variable locale et la donner aussi à la session :

```cpp
            const OutputProfile prof = OutputProfile::detect(
                hello_seen->term, hello_seen->colorterm, hello_seen->utf8);
            client->differ = std::make_unique<Differ>(prof);
            session.set_output(prof);
```

C'est la seule modification de `daemon.cpp` dans cette tâche. Le nouveau client repart d'un `Differ` neuf, donc d'un repeint complet : un changement de thème en cours de route n'a rien à invalider.

- [ ] **Step 7: Lancer toute la suite**

```bash
cmake --build build-release -j"$(nproc)" && ./build-release/sshos_tests
```

Attendu : **232 cas, 0 en echec**. Vérifier nommément que ces huit-là passent **sans avoir été touchés** — c'est la preuve que la migration n'a rien perdu :

```
session_draws_a_panel_on_the_last_row
session_clock_follows_daylight_saving_under_a_single_timezone
session_survives_a_terminal_smaller_than_the_minimum
session_quits_on_ctrl_q
session_shows_full_warning_when_width_permits_but_height_does_not
session_shows_short_warning_when_width_is_too_small_for_the_full_message
daemon_processes_input_sent_just_before_the_client_closes
daemon_honours_a_hello_coalesced_with_its_senders_closure
```

- [ ] **Step 8: Faire tourner vingt fois les trois tests du round EPOLLHUP**

Ils gèlent le démon par `SIGSTOP`, qui est **asynchrone** : `kill()` rend la main avant l'arrêt effectif. Une exécution unique ne prouve rien sur ces trois-là.

```bash
for i in $(seq 1 20); do
  ./build-release/sshos_tests daemon_processes_input_sent_just_before | tail -1
  ./build-release/sshos_tests daemon_keeps_clicks_sent_just_before | tail -1
  ./build-release/sshos_tests daemon_honours_a_hello_coalesced | tail -1
done
```

Attendu : soixante lignes de bilan, toutes à `0 en echec`. Une seule ligne différente arrête la tâche.

- [ ] **Step 9: Passer les sanitizers**

```bash
cmake --build build-dbg -j"$(nproc)" && \
  UBSAN_OPTIONS=halt_on_error=1:print_stacktrace=1 ./build-dbg/sshos_tests
```

- [ ] **Step 10: Vérifier à l'œil, à travers SSH**

C'est la première tâche du jalon dont le résultat se voit. Lancer le démon, s'attacher, et confirmer : une fenêtre décorée avec le titre `Bloc`, ses trois boutons calés à droite, une poignée dans le coin bas-droit, `taille: 42x12` à l'intérieur, un panneau en bas avec la marque à gauche et l'heure à droite. Cliquer dans la fenêtre incrémente `clics:`. `Ctrl+Q` quitte.

- [ ] **Step 11: Commit**

```bash
git add src/daemon/host.hpp src/daemon/host.cpp src/daemon/session.hpp \
        src/daemon/session.cpp src/daemon/daemon.cpp tests/test_session.cpp
git commit -m "feat(daemon): session recomposee - une fenetre decoree, une vraie app, un panneau"
```

---

### Task 5: Hit-test et glissement

Le hit-test doit être l'inverse **exact** des décorations : si `decor_metrics()` peint un bouton en colonne X, `hit_window()` doit répondre « bouton » en colonne X, jamais X+1. Les deux fonctions vivent dans deux fichiers différents et rien dans le compilateur ne les tient d'accord — c'est un test de propriété qui s'en charge.

Ce test doit avoir des dents. La spec (§12.1, famille 2) proposait de faire rendre à `Session` une grille de propriétaires de cellules et de la comparer au hit-test. Ce plan s'en écarte : cette grille serait remplie à partir des **mêmes rectangles** que le hit-test interroge, et comparerait donc une formule à elle-même. On confronte à la place le hit-test aux **glyphes réellement posés sur la surface** — un décalage d'une colonne entre le dessin et le test devient un échec immédiat, et aucun paramètre réservé aux tests ne s'invite dans le code de production.

Trois choses que la spec laissait ouvertes et que ce plan tranche, après vérification dans le code :

**Le mouvement sans bouton.** `src/input/parser.cpp:335` encode le bouton sur `cb & 3` : les valeurs 0, 1 et 2 sont gauche, milieu et droite, et **3 signifie « aucun bouton »**. Un `Motion` avec `button == 3` pendant un glissement veut dire que le relâchement s'est perdu — c'est l'un des sept chemins d'annulation.

**Le chien de garde n'a pas de minuterie où s'accrocher.** La spec parlait de « deux ticks du `timerfd` 1 s ». Ce `timerfd` n'existe pas : `daemon.cpp:97` n'en crée qu'un, réarmé à chaque tour sur l'horloge de frame à 33 ms (`daemon.cpp:552`), et seulement quand quelque chose est sale. Le garde-fou est donc **temporel, pas cadencé** : `Session` note l'instant du dernier événement de glissement et abandonne au-delà de deux secondes, vérifié en tête de `on_input()` et de `render()`.

**Il faut une horloge monotone.** `Platform::now()` rend un `system_clock`, c'est-à-dire une heure murale qu'un `ntpd` peut reculer — inutilisable pour un délai. `Platform` gagne `steady_now()`, ce qui rend au passage le test du garde-fou déterministe au lieu de le faire dormir deux secondes.

**Files:**
- Create: `src/wm/hittest.hpp`, `src/wm/hittest.cpp`
- Modify: `src/common/platform.hpp`
- Modify: `src/daemon/session.hpp`, `src/daemon/session.cpp`
- Modify: `src/daemon/daemon.cpp` (deux appels à `cancel_drag()`)
- Create: `tests/test_hittest.cpp`
- Modify: `tests/test_session.cpp`

**Interfaces:**
- Consumes: `DecorMetrics`, `decor_metrics`, `Window`, `client_rect`, `clamp_to`, `frame_min` (tâches 3 et 4).
- Produces:
  - `enum class sshos::WinHit { None, Frame, TitleBar, ButtonMinimize, ButtonMaximize, ButtonClose, EdgeRight, EdgeBottom, CornerBR, Client };`
  - `struct sshos::WinHitResult { WinHit what = WinHit::None; WindowId win = 0; int lx = 0, ly = 0; };`
  - `WinHitResult sshos::hit_window(const Window& w, int x, int y);`
  - `std::chrono::steady_clock::time_point sshos::Platform::steady_now() const` (virtuelle pure, implémentée par `RealPlatform`).
  - `WinHitResult sshos::Session::hit_window_at(int x, int y) const;`
  - `void sshos::Session::cancel_drag();`

`WinHit::Frame` ne figure pas dans l'énumération de la spec. Il est nécessaire : la bordure **gauche** appartient à la fenêtre sans être une poignée. La rattacher à `TitleBar`, seule autre valeur qui aurait pu l'accueillir, en ferait une zone de déplacement par accident.

- [ ] **Step 1: Écrire le test qui échoue**

Créer `tests/test_hittest.cpp` :

```cpp
#include <memory>
#include <string>

#include "apps/bloc.hpp"
#include "harness.hpp"
#include "render/surface.hpp"
#include "render/theme.hpp"
#include "wm/decor.hpp"
#include "wm/hittest.hpp"
#include "wm/window.hpp"

using sshos::Bloc;
using sshos::Border;
using sshos::DecorMetrics;
using sshos::Rect;
using sshos::Surface;
using sshos::Theme;
using sshos::View;
using sshos::WinHit;
using sshos::Window;
using sshos::decor_metrics;
using sshos::draw_decor;
using sshos::hit_window;

namespace {

Window make_window(Rect r, std::string title) {
  Window w;
  w.id = 7;
  w.title = std::move(title);
  w.user_rect = r;
  w.display_rect = r;
  w.app = std::make_unique<Bloc>();
  return w;
}

}  // namespace

TEST(hit_window_ignores_a_point_outside_the_frame) {
  Window w = make_window(Rect{10, 5, 20, 8}, "Bloc");
  CHECK(hit_window(w, 9, 5).what == WinHit::None);
  CHECK(hit_window(w, 30, 5).what == WinHit::None);
  CHECK(hit_window(w, 10, 4).what == WinHit::None);
  CHECK(hit_window(w, 10, 13).what == WinHit::None);
}

TEST(hit_window_names_every_zone) {
  Window w = make_window(Rect{10, 5, 20, 8}, "Bloc");
  const DecorMetrics m = decor_metrics(w.display_rect);
  REQUIRE(m.button_count == 3);

  CHECK(hit_window(w, 11, 5).what == WinHit::TitleBar);
  CHECK(hit_window(w, m.buttons.x, 5).what == WinHit::ButtonMinimize);
  CHECK(hit_window(w, m.buttons.x + 3, 5).what == WinHit::ButtonMaximize);
  CHECK(hit_window(w, m.buttons.x + 6, 5).what == WinHit::ButtonClose);
  CHECK(hit_window(w, 29, 8).what == WinHit::EdgeRight);
  CHECK(hit_window(w, 15, 12).what == WinHit::EdgeBottom);
  CHECK(hit_window(w, 29, 12).what == WinHit::CornerBR);
  CHECK(hit_window(w, 10, 8).what == WinHit::Frame);  // bordure gauche

  const sshos::WinHitResult c = hit_window(w, 13, 7);
  CHECK(c.what == WinHit::Client);
  CHECK_EQ(c.win, static_cast<sshos::WindowId>(7));
  CHECK_EQ(c.lx, 2);
  CHECK_EQ(c.ly, 1);
}

// LE test de propriété du jalon. On confronte le hit-test aux glyphes
// réellement posés, pas à une seconde dérivation de la même formule : un
// décalage d'une colonne entre decor.cpp et hittest.cpp échoue ici.
// Bordures Unicode volontairement, pour que les glyphes de boutons
// (□ ×) ne puissent pas se confondre avec une lettre du titre.
TEST(hit_test_agrees_with_the_glyphs_actually_painted) {
  for (int w_px = 16; w_px <= 40; ++w_px) {
    Surface s(60, 20);
    Window w = make_window(Rect{3, 2, w_px, 9}, "Bloc");
    View v = s.root();
    draw_decor(v, w, true, Theme::mono16(), Border::Unicode);

    const int y = w.display_rect.y;
    for (int x = w.display_rect.x; x < w.display_rect.x + w_px; ++x) {
      const WinHit what = hit_window(w, x, y).what;
      const bool in_button_zone = (what == WinHit::ButtonMinimize ||
                                   what == WinHit::ButtonMaximize ||
                                   what == WinHit::ButtonClose);
      const char32_t ch = s.at(x, y).ch;
      const bool looks_like_button =
          (ch == U'[' || ch == U']' || ch == U'_' || ch == U'\u25a1' || ch == U'\u00d7');
      CHECK_EQ(in_button_zone, looks_like_button);
    }
  }
}

// Toute cellule du cadre appartient à une zone : aucune ne doit tomber
// dans None, sans quoi un clic dessus traverserait la fenêtre jusqu'au
// bureau.
TEST(hit_window_leaves_no_hole_inside_the_frame) {
  Window w = make_window(Rect{2, 1, 24, 9}, "Bloc");
  for (int y = 1; y < 10; ++y) {
    for (int x = 2; x < 26; ++x) {
      CHECK(hit_window(w, x, y).what != WinHit::None);
    }
  }
}
```

Ajouter à `tests/test_session.cpp` :

```cpp
namespace {

// Presse, bouge, relâche. Le bouton 0 est le gauche (parser.cpp:335).
void press_at(Session& s, int x, int y) {
  sshos::MouseEvent m;
  m.action = sshos::MouseAction::Press;
  m.button = 0;
  m.x = x;
  m.y = y;
  s.on_input(sshos::InputEvent{m});
}

void motion_to(Session& s, int x, int y, uint8_t button = 0) {
  sshos::MouseEvent m;
  m.action = sshos::MouseAction::Motion;
  m.button = button;
  m.x = x;
  m.y = y;
  s.on_input(sshos::InputEvent{m});
}

void release_at(Session& s, int x, int y) {
  sshos::MouseEvent m;
  m.action = sshos::MouseAction::Release;
  m.button = 0;
  m.x = x;
  m.y = y;
  s.on_input(sshos::InputEvent{m});
}

// La géométrie du cadre, relevée sur la surface : la première ligne qui
// porte le titre.
int title_row_of(const Surface& s, int rows) {
  for (int y = 0; y < rows; ++y) {
    if (s.text_row(y).find("Bloc") != std::string::npos) return y;
  }
  return -1;
}

}  // namespace

TEST(session_moves_a_window_dragged_by_its_title_bar) {
  FakePlatform plat;
  Session sess(plat, 80, 24);
  Surface s(80, 24);
  sess.render(s);
  REQUIRE_EQ(title_row_of(s, 24), 1);

  press_at(sess, 5, 1);
  motion_to(sess, 8, 6);
  release_at(sess, 8, 6);

  Surface after(80, 24);
  sess.render(after);
  CHECK_EQ(title_row_of(after, 24), 6);
}

// Le contrat central du glissement de redimensionnement : pendant le
// geste, seul un contour bouge. L'application n'apprend sa nouvelle taille
// qu'au relâchement -- UNE fois, quel que soit le nombre de mouvements.
TEST(session_tells_the_app_its_new_size_exactly_once_per_resize_gesture) {
  FakePlatform plat;
  Session sess(plat, 80, 24);
  Surface s(80, 24);
  sess.render(s);  // resize: 1

  press_at(sess, 45, 14);  // coin bas-droit du cadre {2,1,44,14}
  for (int i = 0; i < 10; ++i) motion_to(sess, 45 + i, 14 + (i % 3));
  release_at(sess, 54, 16);

  Surface after(80, 24);
  sess.render(after);
  bool twice = false;
  for (int y = 0; y < 23; ++y) {
    if (after.text_row(y).find("resize: 2") != std::string::npos) twice = true;
  }
  CHECK(twice);
}

// Les sept chemins d'annulation. Ils asseyent TOUS la même conclusion : la
// fenêtre est restée là où elle était. Un seul chemin oublié laisserait un
// glissement fantôme capable de déplacer une fenêtre au prochain mouvement
// de souris, longtemps après que l'utilisateur a lâché le bouton.
TEST(session_cancels_a_drag_on_every_one_of_the_seven_paths) {
  const int kPaths = 7;
  for (int path = 0; path < kPaths; ++path) {
    FakePlatform plat;
    Session sess(plat, 80, 24);
    Surface s(80, 24);
    sess.render(s);
    REQUIRE_EQ(title_row_of(s, 24), 1);

    press_at(sess, 5, 1);
    motion_to(sess, 8, 6);  // le glissement est engagé

    switch (path) {
      case 0:  // Échap
        sess.on_input(sshos::InputEvent{sshos::KeyEvent{sshos::Key::Escape, 0, 0}});
        break;
      case 1:  // n'importe quelle autre frappe
        sess.on_input(
            sshos::InputEvent{sshos::KeyEvent{sshos::Key::Char, U'z', 0}});
        break;
      case 2:  // perte de focus du terminal
        sess.on_input(sshos::InputEvent{sshos::FocusEvent{false}});
        break;
      case 3:  // mouvement sans bouton : le relâchement s'est perdu
        motion_to(sess, 20, 15, 3);
        break;
      case 4:  // détachement du client, puis attache du suivant : DEUX sites
               // d'appel dans le démon, une seule méthode ici. Le câblage
               // des deux sites est couvert par le test bout-en-bout
               // ci-dessous, pas par ce cas.
        sess.cancel_drag();
        break;
      case 5:  // clic sur un bouton de décoration pendant le glissement
        press_at(sess, 45, 1);
        break;
      case 6: {  // chien de garde : plus de deux secondes sans nouvelle
        plat.advance_steady(std::chrono::milliseconds(2100));
        Surface tick(80, 24);
        sess.render(tick);
        break;
      }
      default:
        break;
    }

    // Après annulation, un mouvement de souris ne doit plus rien traîner.
    motion_to(sess, 40, 20);
    release_at(sess, 40, 20);

    Surface after(80, 24);
    sess.render(after);
    CHECK_EQ(title_row_of(after, 24), 1);
  }
}

// Le cas 4 ci-dessus appelle cancel_drag() en direct : il prouve la
// méthode, pas les DEUX sites d'appel du démon. C'est ce test-ci qui les
// couvre -- un client qui s'en va en plein glissement, et le suivant qui
// hérite d'un bureau propre plutôt que d'un geste à moitié fait.
TEST(daemon_forgets_a_drag_left_behind_by_a_departed_client) {
  const std::string name = unique_name();
  DaemonHandle d(name);
  REQUIRE(d.ok());

  {
    Fd c = connect_retry(name);
    REQUIRE(c.get() >= 0);
    HelloMsg hello = make_hello(80, 24);
    hello.utf8 = true;
    REQUIRE(send_all(c.get(), encode(Msg{hello})));
    Decoder dec;
    REQUIRE(wait_for_frame_containing(c.get(), dec, "Bloc", "ssh_os", 2000));

    // Presser sur la barre de titre, bouger, et disparaître sans relâcher.
    REQUIRE(send_all(c.get(), encode(Msg{InputMsg{"\033[<0;6;2M"}})));
    REQUIRE(send_all(c.get(), encode(Msg{InputMsg{"\033[<32;20;10M"}})));
  }  // le client se ferme ici, en plein geste

  Fd c2 = connect_retry(name);
  REQUIRE(c2.get() >= 0);
  HelloMsg hello2 = make_hello(80, 24);
  hello2.utf8 = true;
  REQUIRE(send_all(c2.get(), encode(Msg{hello2})));
  Decoder dec2;
  REQUIRE(wait_for_frame_containing(c2.get(), dec2, "Bloc", "ssh_os", 2000));

  // Un mouvement bouton enfoncé : si le glissement avait survécu au
  // détachement, la fenêtre suivrait le curseur jusqu'en bas de l'écran.
  REQUIRE(send_all(c2.get(), encode(Msg{InputMsg{"\033[<32;60;18M"}})));

  // Puis un clic à l'intérieur de la zone cliente d'ORIGINE. Il n'atteint
  // Bloc que si la fenêtre n'a pas bougé ; sinon il tombe sur le bureau et
  // le compteur reste à zéro, ce que l'attente traduit en échec.
  REQUIRE(send_all(c2.get(), encode(Msg{InputMsg{"\033[<0;10;5M"}})));
  CHECK(wait_for_frame_containing(c2.get(), dec2, "clics: 1", "ssh_os", 2000));
}
```

Les sept chemins, pour mémoire : `Échap`, toute autre frappe, la perte de focus du terminal, un mouvement sans bouton, un second appui, `cancel_drag()` appelée par le démon, et le chien de garde temporel.

`FakePlatform` gagne une horloge monotone réglable. Dans son bloc de définition en tête de `tests/test_session.cpp` :

```cpp
  std::chrono::steady_clock::time_point steady{};
  std::chrono::steady_clock::time_point steady_now() const override { return steady; }
  void advance_steady(std::chrono::milliseconds d) { steady += d; }
```

`FakePlatformAt` reçoit la même méthode `steady_now()` (elle peut rendre un point fixe : aucun de ses tests ne touche au glissement).

- [ ] **Step 2: Lancer pour vérifier que ça échoue**

```bash
cmake --build build-release -j"$(nproc)"
```

Attendu : ÉCHEC, `wm/hittest.hpp: No such file or directory`.

- [ ] **Step 3: Ajouter `steady_now()` à `Platform`**

Dans `src/common/platform.hpp`, ajouter à `Platform` puis à `RealPlatform` :

```cpp
  // now() rend une heure MURALE, qu'un ajustement d'horloge peut faire
  // reculer : inutilisable pour mesurer un délai. Tout ce qui expire dans
  // ce projet se mesure ici.
  virtual std::chrono::steady_clock::time_point steady_now() const = 0;
```

```cpp
  std::chrono::steady_clock::time_point steady_now() const override {
    return std::chrono::steady_clock::now();
  }
```

Tout double de `Platform` déjà présent dans la suite doit gagner la méthode : chercher `: Platform` et `: sshos::Platform` dans `tests/`.

- [ ] **Step 4: Écrire `src/wm/hittest.hpp` et `src/wm/hittest.cpp`**

`src/wm/hittest.hpp` :

```cpp
#pragma once

#include "wm/window.hpp"

namespace sshos {

// Une valeur par zone cliquable d'une fenêtre. Frame couvre ce qui
// appartient à la fenêtre sans être une poignée -- la bordure gauche : un
// clic dessus la met au premier plan, rien de plus.
enum class WinHit {
  None, Frame, TitleBar,
  ButtonMinimize, ButtonMaximize, ButtonClose,
  EdgeRight, EdgeBottom, CornerBR,
  Client,
};

struct WinHitResult {
  WinHit what = WinHit::None;
  WindowId win = 0;
  int lx = 0;  // coordonnées locales à la zone cliente, valables si Client
  int ly = 0;
};

// L'inverse exact de decor_metrics() + draw_decor(). L'ordre des tests
// ci-dessous suit celui du dessin, du plus spécifique au plus général :
// c'est ce qui garantit qu'ils ne divergent pas.
WinHitResult hit_window(const Window& w, int x, int y);

}  // namespace sshos
```

`src/wm/hittest.cpp` :

```cpp
#include "wm/hittest.hpp"

#include "wm/decor.hpp"

namespace sshos {

WinHitResult hit_window(const Window& w, int x, int y) {
  const Rect f = w.display_rect;
  WinHitResult r;
  if (!f.contains(x, y)) return r;

  r.win = w.id;
  const DecorMetrics m = decor_metrics(f);

  // Le coin d'abord : il appartient à la fois au bord droit et au bord
  // bas, et c'est lui qui redimensionne dans les deux sens à la fois.
  if (x == f.x + f.w - 1 && y == f.y + f.h - 1 && f.h >= 2) {
    r.what = WinHit::CornerBR;
    return r;
  }

  if (y == m.title_bar.y) {
    if (m.button_count > 0 && x >= m.buttons.x && x < m.buttons.x + m.buttons.w) {
      const int slot = (x - m.buttons.x) / 3;          // 0..button_count-1
      const int which = 3 - m.button_count + slot;      // 0 réduire, 2 fermer
      r.what = (which == 0)   ? WinHit::ButtonMinimize
               : (which == 1) ? WinHit::ButtonMaximize
                              : WinHit::ButtonClose;
      return r;
    }
    r.what = WinHit::TitleBar;
    return r;
  }

  if (x == f.x + f.w - 1) {
    r.what = WinHit::EdgeRight;
    return r;
  }
  if (y == f.y + f.h - 1) {
    r.what = WinHit::EdgeBottom;
    return r;
  }

  const Rect c = m.client;
  if (c.contains(x, y)) {
    r.what = WinHit::Client;
    r.lx = x - c.x;
    r.ly = y - c.y;
    return r;
  }

  // Ce qui reste : la bordure gauche.
  r.what = WinHit::Frame;
  return r;
}

}  // namespace sshos
```

- [ ] **Step 5: La machine à états du glissement dans `Session`**

Dans `src/daemon/session.hpp`, ajouter les inclusions `<chrono>`, `<variant>` et `"wm/hittest.hpp"`, puis :

```cpp
 public:
  WinHitResult hit_window_at(int x, int y) const;

  // Appelée par le démon au détachement d'un client et à l'attache du
  // suivant. Un glissement engagé n'a plus de sens quand la souris qui le
  // tenait a disparu.
  void cancel_drag();

 private:
  struct Idle {};
  struct Moving {
    WindowId win = 0;
    int grab_dx = 0;
    int grab_dy = 0;
  };
  struct Resizing {
    WindowId win = 0;
    Rect outline{};
  };
  using DragState = std::variant<Idle, Moving, Resizing>;

  void on_mouse(const MouseEvent& m);
  void watchdog();

  DragState drag_{Idle{}};
  std::chrono::steady_clock::time_point drag_stamp_{};
```

Dans `src/daemon/session.cpp` :

```cpp
namespace {
// Le relâchement peut se perdre : terminal qui filtre, multiplexeur qui
// avale un octet, client tué en plein geste. Deux secondes sans nouvelle
// et le glissement est abandonné. Il n'y a pas de minuterie dédiée où
// s'accrocher -- le démon n'en a qu'une, l'horloge de frame à 33 ms, et
// elle ne bat que lorsque quelque chose est sale -- donc le garde-fou est
// temporel, relu à chaque entrée et à chaque composition.
constexpr std::chrono::milliseconds kDragWatchdog{2000};
}  // namespace

void Session::cancel_drag() { drag_ = Idle{}; }

void Session::watchdog() {
  if (std::holds_alternative<Idle>(drag_)) return;
  if (plat_->steady_now() - drag_stamp_ >= kDragWatchdog) cancel_drag();
}

WinHitResult Session::hit_window_at(int x, int y) const {
  if (!win_) return WinHitResult{};
  return hit_window(*win_, x, y);
}

void Session::on_mouse(const MouseEvent& m) {
  if (!win_) return;
  Window& w = *win_;

  // Un mouvement sans bouton pendant un glissement veut dire que le
  // relâchement s'est perdu (parser.cpp:335 : `cb & 3`, donc 3 = aucun
  // bouton).
  if (m.action == MouseAction::Motion && m.button == 3) {
    cancel_drag();
    return;
  }

  // Un second appui pendant un glissement l'annule. On ne sait pas ce que
  // l'utilisateur veut, et continuer à traîner la fenêtre est le pire des
  // paris possibles.
  if (m.action == MouseAction::Press && !std::holds_alternative<Idle>(drag_)) {
    cancel_drag();
    return;
  }

  if (auto* mv = std::get_if<Moving>(&drag_)) {
    drag_stamp_ = plat_->steady_now();
    Rect r = w.user_rect;
    r.x = m.x - mv->grab_dx;
    r.y = m.y - mv->grab_dy;
    w.user_rect = r;
    if (m.action == MouseAction::Release) cancel_drag();
    return;
  }

  if (auto* rz = std::get_if<Resizing>(&drag_)) {
    drag_stamp_ = plat_->steady_now();
    rz->outline.w = m.x - rz->outline.x + 1;
    rz->outline.h = m.y - rz->outline.y + 1;
    if (m.action == MouseAction::Release) {
      // C'est ICI, et seulement ici, que la fenêtre change de taille : d'où
      // un unique on_resize() par geste, au lieu d'un par mouvement.
      w.user_rect = rz->outline;
      cancel_drag();
    }
    return;
  }

  if (m.action != MouseAction::Press) return;

  const WinHitResult h = hit_window(w, m.x, m.y);
  drag_stamp_ = plat_->steady_now();
  switch (h.what) {
    case WinHit::TitleBar:
      drag_ = Moving{w.id, m.x - w.display_rect.x, m.y - w.display_rect.y};
      break;
    case WinHit::EdgeRight:
    case WinHit::EdgeBottom:
    case WinHit::CornerBR:
      drag_ = Resizing{w.id, w.display_rect};
      break;
    case WinHit::Client: {
      MouseEvent local = m;
      local.x = h.lx;
      local.y = h.ly;
      w.app->on_mouse(local);
      break;
    }
    default:
      break;
  }
}
```

`on_input` remplace son ancien bloc souris par `on_mouse(*m)`, et gagne l'annulation au clavier et à la perte de focus :

```cpp
void Session::on_input(const InputEvent& e) {
  watchdog();

  if (const auto* k = std::get_if<KeyEvent>(&e)) {
    // Toute frappe annule un glissement en cours -- Échap comme les
    // autres. Continuer à déplacer une fenêtre pendant que l'utilisateur
    // tape serait au mieux surprenant.
    if (!std::holds_alternative<Idle>(drag_)) {
      cancel_drag();
      return;
    }
    if (k->key == Key::Char && k->ch == U'q' && (k->mods & mod::Ctrl) != 0) {
      quit_ = true;
      return;
    }
    if (win_) win_->app->on_key(*k);
    return;
  }
  if (const auto* f = std::get_if<FocusEvent>(&e)) {
    if (!f->focused) cancel_drag();
    return;
  }
  if (const auto* m = std::get_if<MouseEvent>(&e)) on_mouse(*m);
}
```

`render()` appelle `watchdog()` en première ligne, et dessine le contour élastique juste après l'application :

```cpp
  if (const auto* rz = std::get_if<Resizing>(&drag_)) {
    Style ol;
    ol.fg = theme_.accent;
    ol.bg = theme_.desktop_bg;
    Rect o = clamp_to(rz->outline, work, frame_min(*w.app));
    v.box(o, border(), ol);
  }
```

- [ ] **Step 6: Câbler `cancel_drag()` dans le démon**

Dans `src/daemon/daemon.cpp`, appeler `session.cancel_drag();` à deux endroits : dans `drop_client()` (le client qui tenait la souris s'en va) et juste après `session.resize(hello_seen->cols, hello_seen->rows);` (un nouveau client hérite d'un bureau propre, pas d'un geste à moitié fait).

- [ ] **Step 7: Lancer les tests**

```bash
cmake --build build-release -j"$(nproc)" && ./build-release/sshos_tests
```

Attendu : **239 cas, 0 en echec**.

- [ ] **Step 8: Discriminer chacun des sept chemins d'annulation**

La règle de la spec (§12.2) : une garde n'est acquise que si un test échoue **sans elle**. Pour chacun des sept `case` du test, retirer la ligne d'annulation correspondante dans `session.cpp`, recompiler, vérifier que `session_cancels_a_drag_on_every_one_of_the_seven_paths` échoue, puis remettre la ligne. Sept mutations, sept échecs observés. **Consigner la sortie d'échec exacte de chacune dans le rapport de tâche.** Un chemin qui passe encore sans sa garde n'est pas couvert : le dire plutôt que d'ajuster le test.

Vérifier l'intégrité du fichier après restauration :

```bash
sha256sum src/daemon/session.cpp    # doit égaler la valeur relevée avant les mutations
```

- [ ] **Step 9: Sanitizers, puis commit**

```bash
cmake --build build-dbg -j"$(nproc)" && \
  UBSAN_OPTIONS=halt_on_error=1:print_stacktrace=1 ./build-dbg/sshos_tests

git add src/wm/hittest.hpp src/wm/hittest.cpp src/common/platform.hpp \
        src/daemon/session.hpp src/daemon/session.cpp src/daemon/daemon.cpp \
        tests/test_hittest.cpp tests/test_session.cpp
git commit -m "feat(wm): hit-test, glissement elastique et les sept annulations"
```

---

### Task 6: Pile, focus, modes, cascade, aimantation, plafond

`Session` passe d'une fenêtre à plusieurs. Le gestionnaire tient la pile — de l'arrière vers l'avant — et le focus, ouvre et ferme, place les nouvelles venues en cascade, aimante les bords, et refuse d'en ouvrir plus de 64.

Une décision de représentation qui n'est pas cosmétique : **les fenêtres vivent dans un `std::vector<std::unique_ptr<Window>>`**, pas dans un `std::vector<Window>`. Chaque `Window` porte un `HostImpl` qui détient un pointeur vers elle ; un `vector` d'objets déplace ses éléments à chaque réallocation et à chaque réordonnancement de la pile, ce qui laisserait tous les hôtes pointer dans le vide. L'indirection coûte une allocation par fenêtre, plafonnées à 64.

**Files:**
- Create: `src/wm/manager.hpp`, `src/wm/manager.cpp`
- Modify: `src/daemon/session.hpp`, `src/daemon/session.cpp`
- Create: `tests/test_manager.cpp`
- Modify: `tests/test_session.cpp`

**Interfaces:**
- Consumes: `Window`, `WinMode`, `clamp_to`, `frame_min` (tâche 3) ; `hit_window` (tâche 5) ; `App` (tâche 2).
- Produces:
  - `class sshos::WindowManager` :
    - `Window* open(std::unique_ptr<App> app, const Rect& work);` — rend `nullptr` au-delà de 64 fenêtres vivantes. Attribue l'identifiant, place en cascade, met au premier plan et donne le focus. **N'attache pas l'hôte** : c'est l'appelant, seul à savoir le construire, qui le fait.
    - `bool close(WindowId id);`
    - `Window* find(WindowId id);` et `const Window* find(WindowId id) const;`
    - `const std::vector<std::unique_ptr<Window>>& stack() const;` — index 0 = arrière-plan.
    - `void raise(WindowId id);` `void focus(WindowId id);` `WindowId focused() const;`
    - `void focus_next();` `void focus_prev();`
    - `Window* hit(int x, int y);` — parcourt la pile de l'avant vers l'arrière.
    - `void set_mode(WindowId id, WinMode m, const Rect& work);`
    - `static constexpr size_t kMaxWindows = 64;`
  - `Rect sshos::snap(Rect r, const Rect& work, int tolerance);` — aimantation à 1 cellule des quatre bords.

- [ ] **Step 1: Écrire le test qui échoue**

Créer `tests/test_manager.cpp` avec, au minimum, ces cas :

```cpp
#include <memory>

#include "apps/bloc.hpp"
#include "harness.hpp"
#include "wm/manager.hpp"

using sshos::Bloc;
using sshos::Rect;
using sshos::WinMode;
using sshos::Window;
using sshos::WindowId;
using sshos::WindowManager;

namespace {
const Rect kWork{0, 0, 80, 23};
Window* open_bloc(WindowManager& wm) {
  return wm.open(std::make_unique<Bloc>(), kWork);
}
}  // namespace

TEST(manager_gives_every_window_a_fresh_identifier) {
  WindowManager wm;
  Window* a = open_bloc(wm);
  Window* b = open_bloc(wm);
  REQUIRE(a != nullptr);
  REQUIRE(b != nullptr);
  CHECK(a->id != b->id);
  const WindowId gone = a->id;
  CHECK(wm.close(gone));
  Window* c = open_bloc(wm);
  REQUIRE(c != nullptr);
  // Un identifiant n'est JAMAIS réutilisé : une action différée sur `gone`
  // ne doit pas frapper la nouvelle venue.
  CHECK(c->id != gone);
  CHECK(wm.find(gone) == nullptr);
}

TEST(manager_cascades_new_windows_instead_of_stacking_them_exactly) {
  WindowManager wm;
  Window* a = open_bloc(wm);
  Window* b = open_bloc(wm);
  REQUIRE(a != nullptr);
  REQUIRE(b != nullptr);
  CHECK_EQ(b->user_rect.x, a->user_rect.x + 2);
  CHECK_EQ(b->user_rect.y, a->user_rect.y + 1);
}

TEST(manager_wraps_the_cascade_before_it_walks_off_the_work_area) {
  WindowManager wm;
  for (int i = 0; i < 30; ++i) {
    Window* w = open_bloc(wm);
    REQUIRE(w != nullptr);
    CHECK(w->display_rect.x >= kWork.x);
    CHECK(w->display_rect.y >= kWork.y);
    CHECK(w->display_rect.x + w->display_rect.w <= kWork.x + kWork.w);
  }
}

TEST(manager_refuses_to_open_more_than_sixty_four_windows) {
  WindowManager wm;
  for (size_t i = 0; i < WindowManager::kMaxWindows; ++i) {
    REQUIRE(open_bloc(wm) != nullptr);
  }
  CHECK(open_bloc(wm) == nullptr);
  CHECK_EQ(wm.stack().size(), WindowManager::kMaxWindows);

  // Le plafond porte sur les fenêtres VIVANTES : en fermer une doit
  // rouvrir la porte.
  const WindowId first = wm.stack().front()->id;
  CHECK(wm.close(first));
  CHECK(open_bloc(wm) != nullptr);
}

TEST(manager_raises_the_window_it_focuses) {
  WindowManager wm;
  Window* a = open_bloc(wm);
  Window* b = open_bloc(wm);
  REQUIRE(a != nullptr);
  REQUIRE(b != nullptr);
  CHECK_EQ(wm.focused(), b->id);
  CHECK_EQ(wm.stack().back()->id, b->id);

  wm.focus(a->id);
  CHECK_EQ(wm.focused(), a->id);
  CHECK_EQ(wm.stack().back()->id, a->id);
}

// La pile se parcourt de l'avant vers l'arrière : deux fenêtres qui se
// recouvrent, c'est celle du dessus qui reçoit le clic.
TEST(manager_hits_the_topmost_window_first) {
  WindowManager wm;
  Window* a = open_bloc(wm);
  Window* b = open_bloc(wm);
  REQUIRE(a != nullptr);
  REQUIRE(b != nullptr);
  a->display_rect = Rect{0, 0, 30, 10};
  b->display_rect = Rect{5, 2, 30, 10};
  Window* hit = wm.hit(10, 5);
  REQUIRE(hit != nullptr);
  CHECK_EQ(hit->id, b->id);
}

TEST(manager_closing_the_focused_window_moves_focus_to_the_one_below) {
  WindowManager wm;
  Window* a = open_bloc(wm);
  Window* b = open_bloc(wm);
  REQUIRE(a != nullptr);
  REQUIRE(b != nullptr);
  CHECK(wm.close(b->id));
  CHECK_EQ(wm.focused(), a->id);
}

TEST(manager_focus_next_skips_minimized_windows) {
  WindowManager wm;
  Window* a = open_bloc(wm);
  Window* b = open_bloc(wm);
  Window* c = open_bloc(wm);
  REQUIRE(a != nullptr);
  REQUIRE(b != nullptr);
  REQUIRE(c != nullptr);
  wm.set_mode(b->id, WinMode::Minimized, kWork);
  wm.focus(a->id);
  wm.focus_next();
  CHECK_EQ(wm.focused(), c->id);
}

// Maximiser puis rétablir doit rendre EXACTEMENT la géométrie d'avant :
// c'est user_rect qui la garde, et set_mode ne doit jamais l'écraser.
TEST(manager_restores_the_exact_geometry_after_maximize) {
  WindowManager wm;
  Window* a = open_bloc(wm);
  REQUIRE(a != nullptr);
  const Rect before = a->user_rect;
  wm.set_mode(a->id, WinMode::Maximized, kWork);
  CHECK(a->display_rect == kWork);
  CHECK(a->user_rect == before);
  wm.set_mode(a->id, WinMode::Normal, kWork);
  CHECK(a->display_rect == before);
}

// Une seule fenêtre plein écran à la fois : focaliser une autre ramène la
// précédente à l'état où elle était avant d'y passer.
TEST(manager_lets_only_one_window_be_fullscreen) {
  WindowManager wm;
  Window* a = open_bloc(wm);
  Window* b = open_bloc(wm);
  REQUIRE(a != nullptr);
  REQUIRE(b != nullptr);
  wm.set_mode(a->id, WinMode::Maximized, kWork);
  wm.set_mode(a->id, WinMode::Fullscreen, kWork);
  CHECK(a->mode == WinMode::Fullscreen);

  wm.focus(b->id);
  CHECK(a->mode == WinMode::Maximized);  // rendue à son état d'avant
}

TEST(snap_pulls_a_window_onto_a_nearby_edge_but_not_a_distant_one) {
  const Rect work{0, 0, 80, 23};
  CHECK_EQ(sshos::snap(Rect{1, 5, 20, 8}, work, 1).x, 0);
  CHECK_EQ(sshos::snap(Rect{3, 5, 20, 8}, work, 1).x, 3);
  CHECK_EQ(sshos::snap(Rect{59, 5, 20, 8}, work, 1).x, 60);
}
```

Ajouter à `tests/test_session.cpp` un cas bout du fil, qui prouve que la pile est bien câblée dans la composition :

```cpp
TEST(session_draws_the_focused_window_on_top) {
  FakePlatform plat;
  Session sess(plat, 80, 24);
  Surface s(80, 24);
  sess.render(s);
  sess.open_from_catalog("bloc");
  Surface two(80, 24);
  sess.render(two);

  int titles = 0;
  for (int y = 0; y < 23; ++y) {
    if (two.text_row(y).find("Bloc") != std::string::npos) ++titles;
  }
  CHECK_EQ(titles, 2);  // deux fenêtres, deux barres de titre visibles
}
```

- [ ] **Step 2: Lancer pour vérifier que ça échoue**

```bash
cmake --build build-release -j"$(nproc)"
```

Attendu : ÉCHEC, `wm/manager.hpp: No such file or directory`.

- [ ] **Step 3: Écrire `src/wm/manager.hpp`**

```cpp
#pragma once

#include <cstddef>
#include <memory>
#include <vector>

#include "wm/window.hpp"

namespace sshos {

// Aimantation : un bord à moins de `tolerance` cellules d'un bord de la
// zone de travail s'y colle. Sans elle, « presque aligné » est le résultat
// le plus probable de tout glissement, et il se voit.
Rect snap(Rect r, const Rect& work, int tolerance);

class WindowManager {
 public:
  static constexpr size_t kMaxWindows = 64;

  // Rend nullptr si le plafond est atteint. N'attache PAS l'hôte : seul
  // l'appelant sait le construire, et il doit le faire avant d'appeler
  // app->attach().
  Window* open(std::unique_ptr<App> app, const Rect& work);
  bool close(WindowId id);

  Window* find(WindowId id);
  const Window* find(WindowId id) const;

  // Index 0 = arrière-plan, back() = premier plan.
  const std::vector<std::unique_ptr<Window>>& stack() const { return stack_; }

  void raise(WindowId id);
  void focus(WindowId id);
  WindowId focused() const { return focused_; }
  void focus_next();
  void focus_prev();

  Window* hit(int x, int y);
  void set_mode(WindowId id, WinMode m, const Rect& work);

 private:
  // unique_ptr et non Window : chaque HostImpl tient un pointeur vers SA
  // fenêtre, et un vector d'objets les invaliderait tous à chaque
  // réallocation comme à chaque raise().
  std::vector<std::unique_ptr<Window>> stack_;
  WindowId next_id_ = 1;
  WindowId focused_ = 0;
  int cascade_ = 0;
};

}  // namespace sshos
```

- [ ] **Step 4: Écrire `src/wm/manager.cpp`**

Points à respecter, dans l'ordre où ils se posent :

- `open()` : refuser au-delà de `kMaxWindows`, prendre `next_id_++` (jamais réutilisé), calculer la géométrie de départ `{2 + 2 * cascade_, 1 + cascade_, 44, 14}` puis `clamp_to(..., work, frame_min(*app))`, incrémenter `cascade_` et **le remettre à zéro** dès que la fenêtre suivante sortirait de la zone (`2 + 2 * (cascade_ + 1) + 44 > work.w` ou l'équivalent en hauteur), pousser à l'arrière du vector, `raise` + `focus`.
- `close()` : retirer du vector. Si la fenêtre fermée avait le focus, donner le focus à `stack_.back()` après retrait, ou à 0 s'il ne reste rien.
- `raise()` : déplacer l'élément à la fin par `std::rotate` ou un `erase` + `push_back` du `unique_ptr` — jamais de copie.
- `focus()` : `raise()` puis noter `focused_`. **Avant** de basculer, si une autre fenêtre est en `Fullscreen`, la ramener à son `before_fullscreen`.
- `focus_next()` / `focus_prev()` : parcourir la pile en sautant les `WinMode::Minimized`, en boucle. Ne rien faire si aucune fenêtre n'est éligible.
- `set_mode()` : `user_rect` n'est **jamais** modifié. `Normal` → `display_rect = clamp_to(user_rect, work, frame_min)` ; `Maximized` → `display_rect = work` ; `Fullscreen` → noter `before_fullscreen = mode` **avant** de basculer, `display_rect` sera recalculé par la couche layout (tâche 7) ; `Minimized` → la fenêtre n'est plus composée mais garde tout son état.
- `hit()` : parcourir `stack_` **en sens inverse** et rendre la première dont `display_rect.contains(x, y)`, en sautant les `Minimized`.
- `snap()` : pour chacun des quatre bords, si l'écart est `<= tolerance`, aligner.

- [ ] **Step 5: Brancher le gestionnaire dans `Session`**

`Session` remplace son `std::unique_ptr<Window> win_` par un `WindowManager wm_`, et gagne :

```cpp
  // Ouvre une application du catalogue. Rend 0 si l'identifiant est inconnu
  // ou si le plafond de fenêtres est atteint.
  WindowId open_from_catalog(std::string_view id);
```

`open_from_catalog()` fabrique l'application, appelle `wm_.open()`, **puis** construit le `HostImpl` sur la fenêtre obtenue et appelle `app->attach(*host)` — dans cet ordre, l'hôte a besoin de l'adresse définitive de la fenêtre.

`render()` compose désormais la pile de l'arrière vers l'avant, en sautant les `Minimized`, et passe `focused == w.id` à `draw_decor`. `ensure_window()` devient « ouvrir `bloc` si la pile est vide ». `on_mouse()` interroge `wm_.hit()` avant `hit_window()`, et route les boutons :

- `ButtonClose` → demander la fermeture (le dialogue modal arrive à la tâche 10 ; ici, fermer directement si `can_close().allowed`, ne rien faire sinon).
- `ButtonMinimize` → `set_mode(..., Minimized, work)`.
- `ButtonMaximize` → bascule `Normal` ↔ `Maximized`.
- Un clic n'importe où sur une fenêtre non focalisée lui donne le focus **avant** tout le reste.

Le glissement de la tâche 5 travaille maintenant sur `wm_.find(drag.win)` plutôt que sur l'unique fenêtre, et **abandonne si la fenêtre a disparu entre-temps** — un `find()` qui rend `nullptr` annule le glissement.

- [ ] **Step 6: Lancer les tests, sanitizers, commit**

```bash
cmake --build build-release -j"$(nproc)" && ./build-release/sshos_tests
cmake --build build-dbg -j"$(nproc)" && \
  UBSAN_OPTIONS=halt_on_error=1:print_stacktrace=1 ./build-dbg/sshos_tests

git add src/wm/manager.hpp src/wm/manager.cpp src/daemon/session.hpp \
        src/daemon/session.cpp tests/test_manager.cpp tests/test_session.cpp
git commit -m "feat(wm): pile, focus, modes, cascade, aimantation et plafond de 64"
```

Attendu : **251 cas, 0 en echec**.

---

### Task 7: Disposition, réversibilité, préservation

La propriété que cette tâche installe tient en une phrase : **rétrécir le terminal puis le rétablir doit rendre exactement la disposition d'origine**. Elle est déjà à moitié acquise — `user_rect` n'est jamais écrasé par une contrainte d'affichage depuis la tâche 3 — mais rien ne l'a encore vérifiée à l'échelle, ni sur les modes, ni sous le seuil des 40×12.

**Files:**
- Create: `src/wm/layout.hpp`, `src/wm/layout.cpp`
- Modify: `src/daemon/session.hpp`, `src/daemon/session.cpp`
- Create: `tests/test_layout.cpp`
- Modify: `tests/test_session.cpp`

**Interfaces:**
- Consumes: `WindowManager`, `Window`, `WinMode`, `clamp_to`, `frame_min`.
- Produces:
  - `enum class sshos::PanelEdge { Top, Bottom, Left, Right };`
  - `Rect sshos::work_area(int cols, int rows, PanelEdge edge, int thickness);`
  - `Rect sshos::display_rect_for(const Window& w, const Rect& work, int cols, int rows);` — la projection d'une fenêtre selon son mode.
  - `void sshos::relayout(WindowManager& wm, const Rect& work, int cols, int rows);` — recalcule tous les `display_rect`, **sans jamais toucher aux `user_rect`**.

- [ ] **Step 1: Écrire le test qui échoue**

Créer `tests/test_layout.cpp` :

```cpp
#include <memory>
#include <vector>

#include "apps/bloc.hpp"
#include "harness.hpp"
#include "wm/layout.hpp"
#include "wm/manager.hpp"

using sshos::Bloc;
using sshos::PanelEdge;
using sshos::Rect;
using sshos::WinMode;
using sshos::WindowManager;

TEST(work_area_carves_the_panel_out_of_the_right_edge) {
  CHECK(sshos::work_area(80, 24, PanelEdge::Bottom, 1) == (Rect{0, 0, 80, 23}));
  CHECK(sshos::work_area(80, 24, PanelEdge::Top, 1) == (Rect{0, 1, 80, 23}));
  CHECK(sshos::work_area(80, 24, PanelEdge::Left, 16) == (Rect{16, 0, 64, 24}));
  CHECK(sshos::work_area(80, 24, PanelEdge::Right, 16) == (Rect{0, 0, 64, 24}));
}

// LA propriété de la tâche. Une disposition riche -- plusieurs fenêtres,
// des modes différents -- traverse un rétrécissement violent et doit
// revenir intacte.
TEST(layout_is_reversible_across_a_terminal_shrink_and_regrow) {
  WindowManager wm;
  for (int i = 0; i < 6; ++i) {
    REQUIRE(wm.open(std::make_unique<Bloc>(), sshos::work_area(160, 50, PanelEdge::Bottom, 1)) != nullptr);
  }
  wm.set_mode(wm.stack()[1]->id, WinMode::Maximized,
              sshos::work_area(160, 50, PanelEdge::Bottom, 1));
  wm.set_mode(wm.stack()[3]->id, WinMode::Minimized,
              sshos::work_area(160, 50, PanelEdge::Bottom, 1));

  const Rect big = sshos::work_area(160, 50, PanelEdge::Bottom, 1);
  sshos::relayout(wm, big, 160, 50);

  std::vector<Rect> before_user;
  std::vector<Rect> before_display;
  for (const auto& w : wm.stack()) {
    before_user.push_back(w->user_rect);
    before_display.push_back(w->display_rect);
  }

  const Rect small = sshos::work_area(80, 24, PanelEdge::Bottom, 1);
  sshos::relayout(wm, small, 80, 24);
  for (size_t i = 0; i < wm.stack().size(); ++i) {
    // Les user_rect ne bougent JAMAIS : c'est eux la mémoire de la
    // disposition, et les écraser est le seul moyen de rendre le
    // rétrécissement irréversible.
    CHECK(wm.stack()[i]->user_rect == before_user[i]);
  }

  sshos::relayout(wm, big, 160, 50);
  for (size_t i = 0; i < wm.stack().size(); ++i) {
    CHECK(wm.stack()[i]->user_rect == before_user[i]);
    CHECK(wm.stack()[i]->display_rect == before_display[i]);
  }
}

TEST(display_rect_follows_the_mode) {
  WindowManager wm;
  const Rect work = sshos::work_area(80, 24, PanelEdge::Bottom, 1);
  auto* w = wm.open(std::make_unique<Bloc>(), work);
  REQUIRE(w != nullptr);

  wm.set_mode(w->id, WinMode::Maximized, work);
  CHECK(sshos::display_rect_for(*w, work, 80, 24) == work);

  // Le plein écran recouvre TOUT, panneau compris : c'est ce qui le
  // distingue du maximisé.
  wm.set_mode(w->id, WinMode::Fullscreen, work);
  CHECK(sshos::display_rect_for(*w, work, 80, 24) == (Rect{0, 0, 80, 24}));
}
```

Ajouter à `tests/test_session.cpp` :

```cpp
// Sous 40x12, la session affiche un avertissement -- et ne touche à RIEN.
// Réagrandir doit rendre le bureau tel qu'il était, pas un bureau neuf.
TEST(session_preserves_the_desktop_across_a_terminal_too_small_to_draw_it) {
  FakePlatform plat;
  Session sess(plat, 80, 24);
  Surface big(80, 24);
  sess.render(big);
  sess.open_from_catalog("bloc");
  sess.open_from_catalog("bloc");
  sess.render(big);

  int before = 0;
  for (int y = 0; y < 23; ++y) {
    if (big.text_row(y).find("Bloc") != std::string::npos) ++before;
  }
  REQUIRE(before == 3);

  Surface tiny(20, 5);
  sess.render(tiny);
  CHECK(tiny.text_row(0).find("petit") != std::string::npos);

  Surface again(80, 24);
  sess.render(again);
  int after = 0;
  for (int y = 0; y < 23; ++y) {
    if (again.text_row(y).find("Bloc") != std::string::npos) ++after;
  }
  CHECK_EQ(after, before);
}
```

- [ ] **Step 2: Lancer pour vérifier que ça échoue**

```bash
cmake --build build-release -j"$(nproc)"
```

Attendu : ÉCHEC, `wm/layout.hpp: No such file or directory`.

- [ ] **Step 3: Écrire `src/wm/layout.hpp` et `src/wm/layout.cpp`**

```cpp
#pragma once

#include "wm/manager.hpp"
#include "wm/window.hpp"

namespace sshos {

enum class PanelEdge { Top, Bottom, Left, Right };

// Ce qui reste de l'écran une fois le panneau retiré. Les fenêtres n'ont
// pas le droit d'en sortir -- c'est ce qui garantit que la barre des tâches
// reste atteignable quoi qu'il arrive.
Rect work_area(int cols, int rows, PanelEdge edge, int thickness);

// La projection d'une fenêtre selon son mode. Ne modifie rien : c'est une
// fonction pure, et c'est ce qui rend le redimensionnement réversible.
Rect display_rect_for(const Window& w, const Rect& work, int cols, int rows);

void relayout(WindowManager& wm, const Rect& work, int cols, int rows);

}  // namespace sshos
```

```cpp
#include "wm/layout.hpp"

namespace sshos {

Rect work_area(int cols, int rows, PanelEdge edge, int thickness) {
  switch (edge) {
    case PanelEdge::Top:
      return Rect{0, thickness, cols, rows - thickness};
    case PanelEdge::Bottom:
      return Rect{0, 0, cols, rows - thickness};
    case PanelEdge::Left:
      return Rect{thickness, 0, cols - thickness, rows};
    case PanelEdge::Right:
      return Rect{0, 0, cols - thickness, rows};
  }
  return Rect{0, 0, cols, rows};
}

Rect display_rect_for(const Window& w, const Rect& work, int cols, int rows) {
  switch (w.mode) {
    case WinMode::Maximized:
      return work;
    case WinMode::Fullscreen:
      // Tout l'écran, panneau compris : c'est exactement ce qui le
      // distingue du maximisé.
      return Rect{0, 0, cols, rows};
    case WinMode::Minimized:
    case WinMode::Normal:
      break;
  }
  return clamp_to(w.user_rect, work, frame_min(*w.app));
}

void relayout(WindowManager& wm, const Rect& work, int cols, int rows) {
  for (const auto& w : wm.stack()) {
    w->display_rect = display_rect_for(*w, work, cols, rows);
  }
}

}  // namespace sshos
```

- [ ] **Step 4: Brancher dans `Session`**

`Session` gagne `PanelEdge edge_ = PanelEdge::Bottom;` et `int thickness_ = 1;`, remplace sa méthode privée `work_area()` par un appel à `sshos::work_area(cols, rows, edge_, thickness_)`, et appelle `relayout(wm_, work, out.w(), out.h())` en tête de composition, avant toute décoration. Le calcul de `display_rect` disparaît du corps de `render()` : il n'a plus qu'un seul endroit où vivre.

Une fenêtre en `Fullscreen` **escamote le panneau** : quand la fenêtre focalisée est dans ce mode, `render()` ne dessine pas le panneau.

- [ ] **Step 5: Lancer les tests, sanitizers, commit**

```bash
cmake --build build-release -j"$(nproc)" && ./build-release/sshos_tests
cmake --build build-dbg -j"$(nproc)" && \
  UBSAN_OPTIONS=halt_on_error=1:print_stacktrace=1 ./build-dbg/sshos_tests

git add src/wm/layout.hpp src/wm/layout.cpp src/daemon/session.hpp \
        src/daemon/session.cpp tests/test_layout.cpp tests/test_session.cpp
git commit -m "feat(wm): zone de travail, projection par mode et disposition reversible"
```

Attendu : **256 cas, 0 en echec**.

---

### Task 8: `FdRegistrar`, clés générationnelles, l'application Battement

La tâche qui ouvre l'`epoll` du démon aux applications. C'est la plus délicate du jalon : elle touche `daemon.cpp`, le fichier qui sort tout juste du round EPOLLHUP.

`epoll_event.data` est une **union** : on ne peut pas y ranger un `fd` pour les descripteurs du démon et un `u64` pour ceux des applications. Toute la boucle bascule donc d'un coup sur `data.u64`, avec ce codage :

```
clé = (window_id << 32) | génération
```

`window_id == 0` est réservé au démon lui-même : génération 1 pour l'écoute, 2 pour la minuterie, 3 pour le `signalfd`. Ces trois descripteurs vivent aussi longtemps que le processus. Le client et la connexion en attente prennent une génération issue d'un compteur qui démarre à 16 et **s'incrémente à chaque `accept()`** — c'est ce qui referme le trou documenté à `daemon.cpp:128` : un événement en retard sur un numéro de descripteur déjà recyclé porte l'ancienne génération et n'est reconnu par personne.

Les descripteurs applicatifs prennent le `window_id` de leur fenêtre et une génération **incrémentée à chaque `watch()`**, pas à chaque fenêtre : une même application qui referme et rouvre son tuyau obtient un numéro de descripteur identique et doit malgré tout recevoir une clé neuve.

**Files:**
- Modify: `src/daemon/host.hpp`, `src/daemon/host.cpp`
- Modify: `src/daemon/daemon.cpp` (conversion complète aux clés `u64`)
- Modify: `src/daemon/session.hpp`, `src/daemon/session.cpp`
- Create: `src/apps/battement.hpp`, `src/apps/battement.cpp`
- Modify: `src/app/catalog.cpp`
- Create: `tests/test_host.cpp`
- Modify: `tests/test_app.cpp`, `tests/test_session.cpp`

**Interfaces:**
- Produces:
  - `struct sshos::FdRegistrar { virtual void watch(uint64_t key, int fd, uint32_t events) = 0; virtual void unwatch(int fd) = 0; };`
  - `struct sshos::NullFdRegistrar : FdRegistrar` — ignore tout. Objet nul, pas échafaudage de test : il donne aux cas unitaires de `Session` un registrar valide sans `epoll`.
  - `constexpr uint64_t sshos::make_key(WindowId win, uint32_t gen);` et `constexpr WindowId sshos::key_window(uint64_t key);`
  - `HostImpl(Window& win, FdRegistrar& fds, uint32_t& gen)`, plus `IoStatus deliver(uint64_t key, uint32_t events)`, `bool owns(uint64_t key) const`, `void unwatch_all()`.
  - `Session(Platform& plat, FdRegistrar& fds, int cols, int rows)` — **le constructeur change**, tous les cas unitaires de `Session` gagnent un `NullFdRegistrar` local.
  - `void sshos::Session::on_fd_event(uint64_t key, uint32_t events);`
  - `class sshos::Battement : public App`, plus `int beats() const` pour les tests.

- [ ] **Step 1: Écrire le test qui échoue**

Créer `tests/test_host.cpp` :

```cpp
#include <fcntl.h>
#include <sys/epoll.h>
#include <unistd.h>

#include <memory>
#include <vector>

#include "apps/battement.hpp"
#include "apps/bloc.hpp"
#include "daemon/host.hpp"
#include "harness.hpp"

using sshos::App;
using sshos::Battement;
using sshos::Bloc;
using sshos::FdRegistrar;
using sshos::HostImpl;
using sshos::IoStatus;
using sshos::Window;

namespace {

struct FakeRegistrar : FdRegistrar {
  struct Call {
    uint64_t key = 0;
    int fd = -1;
    bool still_open = false;
  };
  std::vector<Call> watches;
  std::vector<Call> unwatches;

  void watch(uint64_t key, int fd, uint32_t) override {
    watches.push_back({key, fd, true});
  }
  void unwatch(int fd) override {
    // On relève ICI si le descripteur est encore ouvert : c'est la seule
    // façon d'observer l'ordre « unwatch avant close ». L'ordre inverse
    // laisserait une entrée epoll sur un numéro que le noyau peut
    // réattribuer à la milliseconde suivante.
    unwatches.push_back({0, fd, ::fcntl(fd, F_GETFD) >= 0});
  }
};

// La fenêtre porte son hôte, exactement comme en production. Un HostImpl
// local au test serait détruit AVANT la Window qui le référence -- l'ordre
// des destructions dans un bloc est l'inverse des déclarations -- et le
// destructeur de Battement appellerait unwatch() sur un hôte mort. C'est
// précisément ce que l'ordre des membres de Window (host avant app)
// garantit, et le test doit s'appuyer dessus plutôt que le contourner.
// Elle est aussi allouée sur le tas, comme dans WindowManager, et pour la
// même raison : `return w;` sur un objet nommé PEUT déplacer (la NRVO
// n'est pas garantie), et l'HostImpl resterait accroché à l'ancienne
// adresse.
std::unique_ptr<Window> make_window(sshos::WindowId id, std::unique_ptr<App> app,
                                    FdRegistrar& reg, uint32_t& gen) {
  auto w = std::make_unique<Window>();
  w->id = id;
  w->app = std::move(app);
  w->host = std::make_unique<HostImpl>(*w, reg, gen);
  return w;
}

HostImpl& host_of(Window& w) { return *static_cast<HostImpl*>(w.host.get()); }

}  // namespace

TEST(host_hands_out_a_new_key_every_time_even_for_the_same_fd) {
  FakeRegistrar reg;
  uint32_t gen = 16;
  auto w = make_window(3, std::make_unique<Bloc>(), reg, gen);
  HostImpl& host = host_of(*w);

  const uint64_t first = host.watch(7, 0);
  host.unwatch(first);
  const uint64_t second = host.watch(7, 0);  // MÊME numéro de descripteur

  CHECK(first != second);
  CHECK_EQ(sshos::key_window(first), static_cast<sshos::WindowId>(3));
  CHECK_EQ(sshos::key_window(second), static_cast<sshos::WindowId>(3));
}

// LE test qui justifie les générations. Sans elles, cet événement périmé
// serait livré à l'application comme s'il était le sien.
TEST(host_drops_an_event_carrying_a_stale_key) {
  FakeRegistrar reg;
  uint32_t gen = 16;
  auto w = make_window(3, std::make_unique<Bloc>(), reg, gen);
  HostImpl& host = host_of(*w);

  const uint64_t stale = host.watch(7, 0);
  host.unwatch(stale);
  const uint64_t fresh = host.watch(7, 0);

  CHECK(!host.owns(stale));
  CHECK(host.owns(fresh));
}

TEST(host_unwatches_before_the_app_closes_its_descriptor) {
  FakeRegistrar reg;
  uint32_t gen = 16;
  {
    auto w = make_window(4, std::make_unique<Battement>(), reg, gen);
    w->app->attach(host_of(*w));
    REQUIRE(!reg.watches.empty());
  }  // w meurt ici : app d'abord, host ensuite
  REQUIRE(!reg.unwatches.empty());
  for (const auto& u : reg.unwatches) {
    CHECK(u.still_open);
  }
}

TEST(battement_counts_what_it_reads) {
  FakeRegistrar reg;
  uint32_t gen = 16;
  auto w = make_window(5, std::make_unique<Battement>(), reg, gen);
  w->app->attach(host_of(*w));
  REQUIRE_EQ(reg.watches.size(), static_cast<size_t>(1));

  auto* app = static_cast<Battement*>(w->app.get());
  CHECK_EQ(app->beats(), 0);
  app->beat();
  CHECK(host_of(*w).deliver(reg.watches[0].key, EPOLLIN) == IoStatus::Ok);
  CHECK_EQ(app->beats(), 1);
}

// L'écho applicatif du correctif EPOLLHUP du jalon 1 : ce qui est arrivé
// avant la fermeture doit être lu AVANT que la source soit déclarée morte.
TEST(battement_drains_before_it_declares_its_source_dead) {
  FakeRegistrar reg;
  uint32_t gen = 16;
  auto w = make_window(6, std::make_unique<Battement>(), reg, gen);
  w->app->attach(host_of(*w));
  REQUIRE_EQ(reg.watches.size(), static_cast<size_t>(1));

  auto* app = static_cast<Battement*>(w->app.get());
  app->beat();
  app->beat();
  app->beat();
  app->cut_source();  // ferme l'extrémité d'écriture

  // Un seul réveil, portant les deux drapeaux à la fois -- exactement ce que
  // le noyau livre quand des octets et un FIN arrivent groupés.
  const IoStatus st = host_of(*w).deliver(reg.watches[0].key, EPOLLIN | EPOLLHUP);
  CHECK_EQ(app->beats(), 3);
  CHECK(st == IoStatus::Closed);
}
```

Les cas unitaires de `Session` dans `tests/test_session.cpp` gagnent tous un registrar :

```cpp
  sshos::NullFdRegistrar fds;
  Session sess(plat, fds, 40, 12);
```

- [ ] **Step 2: Lancer pour vérifier que ça échoue**

```bash
cmake --build build-release -j"$(nproc)"
```

Attendu : ÉCHEC, `apps/battement.hpp: No such file or directory` et `HostImpl` sans constructeur à trois arguments.

- [ ] **Step 3: `FdRegistrar`, les clés, et `HostImpl` complet**

Dans `src/daemon/host.hpp` :

```cpp
// Couture entre la session et l'epoll du démon. La session ne voit jamais
// le descripteur de l'epoll ; le démon ne sait jamais ce qu'une clé
// désigne.
struct FdRegistrar {
  virtual ~FdRegistrar() = default;
  virtual void watch(uint64_t key, int fd, uint32_t events) = 0;
  // Toujours appelée AVANT close(). L'ordre inverse laisse une entrée sur
  // un numéro que le noyau peut réattribuer aussitôt.
  virtual void unwatch(int fd) = 0;
};

// Objet nul, pas échafaudage : il donne un registrar valide aux cas
// unitaires de Session, qui n'ont pas d'epoll et n'en veulent pas.
struct NullFdRegistrar : FdRegistrar {
  void watch(uint64_t, int, uint32_t) override {}
  void unwatch(int) override {}
};

constexpr uint64_t make_key(WindowId win, uint32_t gen) {
  return (static_cast<uint64_t>(win) << 32) | gen;
}
constexpr WindowId key_window(uint64_t key) {
  return static_cast<WindowId>(key >> 32);
}

// Générations réservées au démon lui-même (window_id == 0). Les trois
// premiers descripteurs vivent aussi longtemps que le processus ; les
// suivants (client, connexion en attente) prennent une génération issue
// d'un compteur qui démarre ici.
inline constexpr uint32_t kGenListener = 1;
inline constexpr uint32_t kGenTimer = 2;
inline constexpr uint32_t kGenSignal = 3;
inline constexpr uint32_t kGenFirstDynamic = 16;
```

`HostImpl` tient `std::vector<std::pair<uint64_t, int>> watched_` (clé → descripteur) :

- `watch(fd, events)` : `key = make_key(win_->id, (*gen_)++)`, enregistre la paire, appelle `fds_->watch(key, fd, events)`, rend la clé.
- `unwatch(token)` : cherche la paire, appelle `fds_->unwatch(fd)`, **retire l'entrée**. Un jeton inconnu est ignoré sans bruit.
- `owns(key)` : la paire existe-t-elle encore.
- `deliver(key, events)` : si `!owns(key)`, rend `IoStatus::Ok` sans rien faire ; sinon délègue à `win_->app->on_io(key, events)`.
- `unwatch_all()` : appelée avant de détruire une fenêtre encore surveillée.

- [ ] **Step 4: Écrire Battement**

`src/apps/battement.hpp` / `.cpp`. L'application factice **avec** descripteur : elle crée un tuyau, surveille l'extrémité de lecture, compte ce qui arrive.

- `attach(Host& h)` : `::pipe2(fds, O_CLOEXEC | O_NONBLOCK)`, garde les deux extrémités, `token_ = h.watch(read_fd_, EPOLLIN)`, `h.set_title("Battement")`.
- `beat()` : écrit un octet dans l'extrémité d'écriture. Appelée par le test et, à la tâche 10, par une commande du menu.
- `cut_source()` : `::close(write_fd_)`, `write_fd_ = -1`. La seconde commande du menu.
- `on_io(token, events)` : **d'abord drainer**, ensuite seulement regarder `EPOLLHUP`. Lire en boucle jusqu'à `EAGAIN` en comptant les octets ; si une lecture rend 0, ou si `EPOLLHUP`/`EPOLLERR` est présent **après** le drain, appeler `host_->unwatch(token_)` puis `::close(read_fd_)` et rendre `IoStatus::Closed`.
- `render(View v)` : affiche `battements: N` et `source: vivante` ou `source: fermee`.
- Destructeur : si le tuyau est encore surveillé, `host_->unwatch(token_)` **avant** de fermer les descripteurs. C'est l'ordre que garantit la déclaration de `host` avant `app` dans `Window` (tâche 3).

Ajouter l'entrée `{"battement", "Battement", &make_battement}` au catalogue.

- [ ] **Step 5: Convertir `daemon.cpp` aux clés `u64`**

Dans l'ordre, en compilant après chaque point :

1. `epoll_mod` et `epoll_add` (lignes ~53 et ~60) prennent un paramètre `uint64_t key` et écrivent `ev.data.u64 = key;` au lieu de `ev.data.fd = fd;`.
2. `struct Client` gagne `uint64_t key = 0;`. À chaque `accept()`, prendre `make_key(0, next_gen_++)` avec `next_gen_` initialisé à `kGenFirstDynamic`.
3. Les enregistrements initiaux passent `make_key(0, kGenListener)`, `make_key(0, kGenTimer)`, `make_key(0, kGenSignal)`.
4. Ligne ~225, `const int fd = evs[i].data.fd;` devient `const uint64_t key = evs[i].data.u64;`. Les comparaisons `fd == sigfd.get()`, `client && fd == client->fd.get()`, `pending && fd == pending->fd.get()` deviennent des comparaisons de clés.
5. `closed_this_batch` contient désormais des clés, pas des descripteurs.
6. **Ajouter une branche finale** : si la clé n'est aucune de celles du démon, `session.on_fd_event(key, events);`. Une clé qu'aucune fenêtre ne reconnaît y est jetée sans bruit.
7. Instancier le registrar concret et le donner à la session :

```cpp
  struct EpollRegistrar : FdRegistrar {
    int ep = -1;
    void watch(uint64_t key, int fd, uint32_t events) override {
      epoll_event ev{};
      ev.events = events;
      ev.data.u64 = key;
      if (::epoll_ctl(ep, EPOLL_CTL_ADD, fd, &ev) < 0 && errno == EEXIST) {
        ::epoll_ctl(ep, EPOLL_CTL_MOD, fd, &ev);
      }
    }
    void unwatch(int fd) override { ::epoll_ctl(ep, EPOLL_CTL_DEL, fd, nullptr); }
  };
```

`ep` ne peut être renseigné qu'après la création de l'`epoll` (ligne ~90), donc déclarer le registrar après elle et construire la `Session` ensuite.

Cette conversion **n'est discriminée par aucun test** contre le code d'avant : l'ancienne dispatche par numéro de descripteur avec un `EPOLL_CTL_DEL` systématique au largage, ce qui referme le trou en pratique. Elle est nécessaire parce que `epoll_event.data` est une union et que les descripteurs applicatifs ont besoin d'un `u64`. Le dire tel quel dans le rapport de tâche ; ce sont `host_drops_an_event_carrying_a_stale_key` et les 189 cas hérités qui gardent cette tâche, pas un test taillé sur mesure.

- [ ] **Step 6: Router dans `Session`**

`Session` prend un `FdRegistrar&` au constructeur, le garde, et le passe à chaque `HostImpl` qu'elle construit avec un compteur de générations qui lui appartient (`uint32_t fd_gen_ = kGenFirstDynamic;`).

```cpp
void Session::on_fd_event(uint64_t key, uint32_t events) {
  Window* w = wm_.find(key_window(key));
  if (w == nullptr || w->host == nullptr) return;  // fenêtre déjà fermée
  auto* host = static_cast<HostImpl*>(w->host.get());
  if (host->deliver(key, events) == IoStatus::Closed) {
    // L'application a perdu sa source. Elle reste vivante : c'est à elle
    // de décider si elle veut continuer, et Battement le fait.
  }
}
```

Fermer une fenêtre appelle `unwatch_all()` sur son hôte **avant** de détruire la fenêtre.

- [ ] **Step 7: Tests, sanitizers, vingt tours des tests EPOLLHUP**

```bash
cmake --build build-release -j"$(nproc)" && ./build-release/sshos_tests
cmake --build build-dbg -j"$(nproc)" && \
  UBSAN_OPTIONS=halt_on_error=1:print_stacktrace=1 ./build-dbg/sshos_tests

for i in $(seq 1 20); do
  ./build-release/sshos_tests daemon_processes_input_sent_just_before | tail -1
  ./build-release/sshos_tests daemon_keeps_clicks_sent_just_before | tail -1
  ./build-release/sshos_tests daemon_honours_a_hello_coalesced | tail -1
done
```

Attendu : **267 cas, 0 en echec**, et soixante bilans à zéro. C'est cette tâche qui touche la boucle du démon : si un seul de ces tours diverge, s'arrêter et diagnostiquer avant de commiter.

- [ ] **Step 8: Commit**

```bash
git add src/daemon src/apps/battement.hpp src/apps/battement.cpp \
        src/app/catalog.cpp tests/test_host.cpp tests/test_app.cpp \
        tests/test_session.cpp
git commit -m "feat(daemon): cles generationnelles, FdRegistrar et application Battement"
```

---

### Task 9: Le panneau complet et l'horloge

Le panneau passe de trois éléments figés à un vrai composant : quatre bords, la liste des fenêtres, le débordement, et une horloge qui ne salit la frame que lorsqu'elle change de texte.

Ce dernier point demande quelque chose que le démon ne sait pas encore faire. Sa minuterie ne bat que lorsqu'une frame est sale (`daemon.cpp:211`, `timeout = renderable ? clock.delay_ms(...) : -1`) : sans réveil périodique, une horloge affichée resterait figée jusqu'à la prochaine frappe. Cette tâche ouvre donc le canal **session → démon** que la tâche 4 avait laissé en suspens.

**Files:**
- Create: `src/shell/panel.hpp`, `src/shell/panel.cpp`
- Create: `src/shell/clock.hpp`, `src/shell/clock.cpp`
- Modify: `src/daemon/session.hpp`, `src/daemon/session.cpp`, `src/daemon/host.cpp`
- Modify: `src/daemon/daemon.cpp` (réveil d'une seconde et prise en compte du drapeau)
- Create: `tests/test_panel.cpp`
- Modify: `tests/test_session.cpp`

**Interfaces:**
- Produces:
  - `enum class sshos::PanelHit { None, Body, MenuButton, Pinned, Task, Overflow, Clock };`
  - `struct sshos::PanelHitResult { PanelHit what = PanelHit::None; int index = -1; WindowId win = 0; };`
  - `class sshos::Panel` : `set_edge(PanelEdge)`, `edge()`, `thickness()`, `rect(int cols, int rows)`, `layout(const WindowManager& wm, int cols, int rows, bool utf8)`, `draw(View v, const Theme& th, const std::string& clock_text)`, `hit(int x, int y) const`.
  - `class sshos::Clock` : `bool update(const Platform& plat)` — rend `true` si le texte a changé ; `const std::string& text() const` ; `const std::string& date() const`.
  - `bool sshos::Session::take_dirty();`

- [ ] **Step 1: Écrire le test qui échoue**

Créer `tests/test_panel.cpp`. Son préambule doit définir son propre double de `Platform` : `FakePlatformAt` vit dans un `namespace` anonyme de `tests/test_session.cpp` (l. 51) et n'est donc pas atteignable depuis un autre fichier. Le dupliquer en dix lignes coûte moins cher que de déplacer les doubles dans un en-tête partagé, ce qui remuerait le fichier le plus sensible de la suite.

```cpp
#include <chrono>
#include <cstdint>
#include <memory>
#include <string>

#include "apps/bloc.hpp"
#include "harness.hpp"
#include "render/surface.hpp"
#include "render/theme.hpp"
#include "shell/clock.hpp"
#include "shell/panel.hpp"
#include "wm/manager.hpp"

using sshos::Bloc;
using sshos::Clock;
using sshos::Panel;
using sshos::PanelEdge;
using sshos::PanelHit;
using sshos::PanelHitResult;
using sshos::Rect;
using sshos::Surface;
using sshos::Theme;
using sshos::View;
using sshos::WinMode;
using sshos::WindowManager;

namespace {

// Double local : FakePlatformAt est enfermé dans le namespace anonyme de
// test_session.cpp.
struct ClockPlatform : sshos::Platform {
  explicit ClockPlatform(std::int64_t epoch_seconds) : t_(epoch_seconds) {}
  std::chrono::system_clock::time_point now() const override {
    return std::chrono::system_clock::time_point(std::chrono::seconds(t_));
  }
  std::chrono::steady_clock::time_point steady_now() const override {
    return std::chrono::steady_clock::time_point{};
  }
  std::string read_file(std::string_view) const override { return {}; }

 private:
  std::int64_t t_;
};

}  // namespace

TEST(panel_thickness_depends_on_the_edge) {
  Panel p;
  p.set_edge(PanelEdge::Bottom);
  CHECK_EQ(p.thickness(), 1);
  p.set_edge(PanelEdge::Left);
  CHECK_EQ(p.thickness(), 16);
}

TEST(panel_occupies_its_edge_and_nothing_else) {
  Panel p;
  p.set_edge(PanelEdge::Top);
  CHECK(p.rect(80, 24) == (Rect{0, 0, 80, 1}));
  p.set_edge(PanelEdge::Right);
  CHECK(p.rect(80, 24) == (Rect{64, 0, 16, 24}));
}

// Chaque fenêtre a son entrée, et l'entrée dit son état : ● pour la
// fenêtre active, _ pour une réduite.
TEST(panel_marks_the_active_and_the_minimized_windows) {
  Panel p;
  p.set_edge(PanelEdge::Bottom);
  WindowManager wm;
  const Rect work{0, 0, 80, 23};
  auto* a = wm.open(std::make_unique<Bloc>(), work);
  auto* b = wm.open(std::make_unique<Bloc>(), work);
  REQUIRE(a != nullptr);
  REQUIRE(b != nullptr);
  wm.set_mode(a->id, WinMode::Minimized, work);
  wm.focus(b->id);
  p.layout(wm, 80, 24, true);

  Surface s(80, 24);
  View v = s.root();
  p.draw(v, Theme::mono16(), "10:05");
  const std::string row = s.text_row(23);
  CHECK(row.find("\xe2\x97\x8f") != std::string::npos);  // ● la fenêtre active
  CHECK(row.find('_') != std::string::npos);             // _ la fenêtre réduite

  // Et le hit-test doit désigner la bonne fenêtre, pas seulement la bonne
  // catégorie.
  bool found_b = false;
  for (int x = 0; x < 80; ++x) {
    const PanelHitResult h = p.hit(x, 23);
    if (h.what == PanelHit::Task && h.win == b->id) found_b = true;
  }
  CHECK(found_b);
}

// Le débordement : au-delà de ce que le bord peut montrer, les libellés
// s'élident jusqu'à huit cellules, et si ça ne suffit toujours pas le
// reste se replie sur un compteur.
TEST(panel_elides_labels_before_it_folds_the_rest_into_a_counter) {
  Panel p;
  p.set_edge(PanelEdge::Bottom);
  WindowManager wm;
  for (int i = 0; i < 30; ++i) {
    REQUIRE(wm.open(std::make_unique<Bloc>(), Rect{0, 0, 80, 23}) != nullptr);
  }
  p.layout(wm, 80, 24, true);

  Surface s(80, 24);
  View v = s.root();
  p.draw(v, Theme::mono16(), "10:05");
  const std::string row = s.text_row(23);
  CHECK(row.find("\xc2\xbb") != std::string::npos);  // le repli »N
}

// Le hit-test du panneau est l'inverse de sa disposition, exactement comme
// celui des fenêtres l'est de leurs décorations. Le bouton de menu occupe
// des cellules précises : cliquer une colonne à côté ne doit pas l'ouvrir.
TEST(panel_hit_test_matches_its_layout) {
  Panel p;
  p.set_edge(PanelEdge::Bottom);
  WindowManager wm;
  REQUIRE(wm.open(std::make_unique<Bloc>(), Rect{0, 0, 80, 23}) != nullptr);
  p.layout(wm, 80, 24, true);

  // Hors du panneau : rien.
  CHECK(p.hit(10, 0).what == PanelHit::None);
  CHECK(p.hit(10, 22).what == PanelHit::None);

  // Sur le panneau : jamais None, chaque cellule appartient à quelque chose.
  int menu_cells = 0;
  int task_cells = 0;
  for (int x = 0; x < 80; ++x) {
    const PanelHitResult h = p.hit(x, 23);
    CHECK(h.what != PanelHit::None);
    if (h.what == PanelHit::MenuButton) ++menu_cells;
    if (h.what == PanelHit::Task) ++task_cells;
  }
  // « ☰ ssh_os » : huit cellules d'affichage, le glyphe en valant une (la
  // politique de largeur ambiguë est à faux, cf. width.cpp:46).
  CHECK_EQ(menu_cells, 8);
  CHECK(task_cells > 0);
}

// L'horloge ne salit la frame que lorsque son TEXTE change. Sans cette
// garde, le bureau se repeindrait trente fois par seconde pour rien, et la
// contre-pression du jalon 1 finirait par s'en apercevoir.
TEST(clock_reports_a_change_only_when_the_rendered_text_changes) {
  ClockPlatform at(1786370700);  // 2026-08-10 14:05:00 UTC
  Clock c;
  CHECK(c.update(at));           // première fois : toujours un changement
  CHECK(!c.update(at));          // même instant, même texte
  ClockPlatform later(1786370700 + 30);
  CHECK(!c.update(later));       // même minute
  ClockPlatform next_minute(1786370700 + 60);
  CHECK(c.update(next_minute));
}
```

- [ ] **Step 2: Lancer, constater l'échec, puis implémenter**

`Panel::layout()` calcule et mémorise la position de chaque élément dans une liste d'`{PanelHit, index, WindowId, Rect}`. `draw()` et `hit()` lisent **cette même liste** — c'est la seule façon de garantir que le hit-test du panneau ne dérive pas de son dessin, et le test `panel_hit_test_matches_its_layout` le prouve.

Éléments, dans l'ordre : le bouton de menu (`☰ ssh_os` en UTF-8, `ssh_os` sinon — la marque reste dans les deux cas, plusieurs tests hérités en dépendent), les entrées épinglées du catalogue, une entrée par fenêtre vivante, l'horloge collée à l'autre bout. Une entrée porte `●` si elle a le focus, `_` si elle est réduite. Les libellés s'élident à huit cellules avant qu'on ne replie le reste sur `»N`.

Sur un bord vertical (`Left`/`Right`), l'épaisseur est de 16 colonnes et les entrées s'empilent verticalement ; l'horloge occupe la dernière ligne du panneau, la date la ligne au-dessus.

`Clock::update()` reconstruit `%H:%M` et `%a %d %b` par `::localtime_r` — **reprendre le corps de `clock_text()` de `session.cpp` avec son commentaire**, qui explique pourquoi `::tzset()` n'est pas cosmétique et pourquoi l'heure est locale. Le déplacer ici, ne pas le réécrire : deux tests hérités en dépendent, dont la preuve de l'heure d'été.

`Session` gagne `bool dirty_ = false;` et `bool take_dirty() { const bool d = dirty_; dirty_ = false; return d; }`. `HostImpl::invalidate()` le lève enfin — remplacer le commentaire posé à la tâche 4 par l'implémentation. `Session::render()` le lève aussi quand `clock_.update(*plat_)` rend `true`.

- [ ] **Step 3: Réveiller le démon une fois par seconde**

Dans `src/daemon/daemon.cpp` :

```cpp
    // Avec un client attaché, on ne dort jamais plus d'une seconde : sans
    // ce plancher, l'horloge du panneau resterait figée jusqu'à la
    // prochaine frappe, la minuterie ne battant que lorsqu'une frame est
    // sale.
    int timeout = renderable ? clock.delay_ms(FrameClock::Clock::now())
                             : (client ? 1000 : -1);
```

et, une fois par tour, avant la décision de rendu :

```cpp
    if (session.take_dirty()) clock.mark_dirty();
```

Ce câblage n'est pas couvert par un test automatique : il demanderait de franchir une vraie limite de minute avec l'horloge réelle du système. Le vérifier à l'étape 5, à l'œil, et le déclarer tel quel dans le rapport de tâche.

- [ ] **Step 4: Tests et sanitizers**

```bash
cmake --build build-release -j"$(nproc)" && ./build-release/sshos_tests
cmake --build build-dbg -j"$(nproc)" && \
  UBSAN_OPTIONS=halt_on_error=1:print_stacktrace=1 ./build-dbg/sshos_tests
```

Attendu : **279 cas, 0 en echec**.

- [ ] **Step 5: Vérifier l'horloge à l'œil**

S'attacher, ne rien toucher, et regarder l'horloge du panneau franchir une minute. Elle doit avancer sans qu'on ait tapé quoi que ce soit — c'est le seul moyen d'observer le réveil d'une seconde. Confirmer aussi que le bureau ne se repeint pas en continu : la sonde CPU du round EPOLLHUP (`hup_cpu_probe`) doit toujours relever **0 jiffie sur 2 s** au repos.

- [ ] **Step 6: Commit**

```bash
git add src/shell/panel.hpp src/shell/panel.cpp src/shell/clock.hpp \
        src/shell/clock.cpp src/daemon tests/test_panel.cpp tests/test_session.cpp
git commit -m "feat(shell): panneau a quatre bords, debordement et horloge qui ne salit qu'au changement"
```

---

### Task 10: Le menu et les raccourcis leader

Le menu et la table de raccourcis arrivent ensemble parce que l'un ouvre l'autre : `Action::OpenMenu` est la seule façon d'atteindre le menu au clavier, et les séparer ferait écrire deux fois le même câblage.

Une correction à l'interface que la spec proposait. Elle donnait `std::optional<Action> feed(const KeyEvent&)`, avec « `nullopt` = touche à transmettre ». Ce type ne suffit pas : quand `Ctrl+A` **arme** le dispatcheur, il n'y a pas d'action à exécuter et la touche ne doit pourtant **pas** être transmise à l'application. Deux états distincts, un seul `nullopt` pour les dire. Le dispatcheur rend donc une paire.

**Files:**
- Create: `src/input/shortcuts.hpp`, `src/input/shortcuts.cpp`
- Create: `src/shell/menu.hpp`, `src/shell/menu.cpp`
- Modify: `src/daemon/session.hpp`, `src/daemon/session.cpp`
- Modify: `src/daemon/daemon.cpp` (flux hors bande et repeint forcé)
- Create: `tests/test_shortcuts.cpp`, `tests/test_menu.cpp`
- Modify: `tests/test_session.cpp`

**Interfaces:**
- Produces:
  - `enum class sshos::Action { MoveLeft, MoveRight, MoveUp, MoveDown, GrowWidth, ShrinkWidth, GrowHeight, ShrinkHeight, NextWindow, PrevWindow, Close, Minimize, MaximizeToggle, FullscreenToggle, OpenMenu, ToggleMouse, ForceRepaint, LiteralLeader };`
  - `struct sshos::LeaderResult { bool consumed = false; std::optional<Action> action; };`
  - `class sshos::LeaderDispatch` : `explicit LeaderDispatch(char32_t leader = U'a')`, `LeaderResult feed(const KeyEvent& k)`, `bool armed() const`.
  - `enum class sshos::MenuHit { None, Body, Search, Item };`
  - `struct sshos::MenuHitResult { MenuHit what = MenuHit::None; int index = -1; };`
  - `class sshos::Menu` : `open()`, `close()`, `bool is_open() const`, `void type(char32_t)`, `void backspace()`, `void move(int delta)`, `const std::vector<MenuItem>& visible() const`, `const MenuItem* selected() const`, `Rect rect(int cols, int rows) const`, `void draw(View v, const Theme& th, Border b) const`, `MenuHitResult hit(int x, int y) const`.
  - `struct sshos::MenuItem { std::string id; std::string label; };`
  - `std::string sshos::Session::take_out_of_band();` et `bool sshos::Session::take_repaint();`

- [ ] **Step 1: Écrire le test qui échoue**

Créer `tests/test_shortcuts.cpp` :

```cpp
#include "harness.hpp"
#include "input/shortcuts.hpp"

using sshos::Action;
using sshos::Key;
using sshos::KeyEvent;
using sshos::LeaderDispatch;
using sshos::LeaderResult;

namespace {
KeyEvent ctrl(char32_t c) { return KeyEvent{Key::Char, c, sshos::mod::Ctrl}; }
KeyEvent plain(char32_t c) { return KeyEvent{Key::Char, c, 0}; }
}  // namespace

// L'ambiguïté que la spec ne pouvait pas exprimer avec un seul optional :
// la touche leader est avalée SANS produire d'action.
TEST(leader_swallows_its_own_key_without_producing_an_action) {
  LeaderDispatch d;
  CHECK(!d.armed());
  const LeaderResult r = d.feed(ctrl(U'a'));
  CHECK(r.consumed);
  CHECK(!r.action.has_value());
  CHECK(d.armed());
}

TEST(leader_passes_an_ordinary_key_straight_through) {
  LeaderDispatch d;
  const LeaderResult r = d.feed(plain(U'x'));
  CHECK(!r.consumed);
  CHECK(!r.action.has_value());
}

TEST(leader_maps_the_armed_key_to_its_action_and_disarms) {
  LeaderDispatch d;
  d.feed(ctrl(U'a'));
  const LeaderResult r = d.feed(plain(U'w'));
  CHECK(r.consumed);
  REQUIRE(r.action.has_value());
  CHECK(*r.action == Action::Close);
  CHECK(!d.armed());
}

// Taper la touche leader deux fois de suite la transmet littéralement --
// sans quoi Ctrl+A deviendrait intapable pour l'application en dessous.
TEST(leader_typed_twice_yields_the_literal_key) {
  LeaderDispatch d;
  d.feed(ctrl(U'a'));
  const LeaderResult r = d.feed(ctrl(U'a'));
  CHECK(r.consumed);
  REQUIRE(r.action.has_value());
  CHECK(*r.action == Action::LiteralLeader);
  CHECK(!d.armed());
}

// Une touche armée sans liaison désarme et n'exécute rien. Elle est
// consommée : l'utilisateur a commencé un accord, il ne s'attend pas à voir
// la lettre apparaître dans son document.
TEST(leader_disarms_on_an_unbound_key_without_leaking_it) {
  LeaderDispatch d;
  d.feed(ctrl(U'a'));
  const LeaderResult r = d.feed(plain(U'\u00e9'));
  CHECK(r.consumed);
  CHECK(!r.action.has_value());
  CHECK(!d.armed());
}

TEST(leader_covers_every_action_of_the_table) {
  const struct {
    KeyEvent k;
    Action a;
  } table[] = {
      {plain(U'h'), Action::MoveLeft},        {plain(U'l'), Action::MoveRight},
      {plain(U'k'), Action::MoveUp},          {plain(U'j'), Action::MoveDown},
      {plain(U'L'), Action::GrowWidth},       {plain(U'H'), Action::ShrinkWidth},
      {plain(U'J'), Action::GrowHeight},      {plain(U'K'), Action::ShrinkHeight},
      {plain(U'n'), Action::NextWindow},      {plain(U'p'), Action::PrevWindow},
      {plain(U'w'), Action::Close},           {plain(U'm'), Action::Minimize},
      {plain(U'x'), Action::MaximizeToggle},  {plain(U'f'), Action::FullscreenToggle},
      {plain(U' '), Action::OpenMenu},        {plain(U'o'), Action::ToggleMouse},
      {plain(U'r'), Action::ForceRepaint},
  };
  for (const auto& e : table) {
    LeaderDispatch d;
    d.feed(ctrl(U'a'));
    const LeaderResult r = d.feed(e.k);
    REQUIRE(r.action.has_value());
    CHECK(*r.action == e.a);
  }
}
```

`tests/test_menu.cpp` couvre : le menu s'ouvre fermé, il liste le catalogue plus les quatre bords de panneau plus les deux commandes de Battement plus « Quitter la session » ; la saisie filtre la liste sur une sous-chaîne insensible à la casse ; la sélection reste dans les bornes après filtrage ; `hit()` est l'inverse de `rect()` + `visible()`.

Enfin, dans `tests/test_session.cpp` :

```cpp
// Le bureau exécute vraiment l'action, pas seulement le dispatcheur.
TEST(session_moves_the_focused_window_with_the_leader_table) {
  FakePlatform plat;
  sshos::NullFdRegistrar fds;
  Session sess(plat, fds, 80, 24);
  Surface s(80, 24);
  sess.render(s);
  const int before = title_row_of(s, 24);
  REQUIRE(before >= 0);

  sess.on_input(sshos::InputEvent{
      sshos::KeyEvent{sshos::Key::Char, U'a', sshos::mod::Ctrl}});
  sess.on_input(sshos::InputEvent{sshos::KeyEvent{sshos::Key::Char, U'j', 0}});

  Surface after(80, 24);
  sess.render(after);
  CHECK_EQ(title_row_of(after, 24), before + 1);
}

// ToggleMouse n'a pas de message de protocole : la bascule voyage dans le
// flux de trames, que le client recopie verbatim.
TEST(session_emits_the_mouse_toggle_out_of_band) {
  FakePlatform plat;
  sshos::NullFdRegistrar fds;
  Session sess(plat, fds, 80, 24);
  Surface s(80, 24);
  sess.render(s);
  CHECK(sess.take_out_of_band().empty());

  sess.on_input(sshos::InputEvent{
      sshos::KeyEvent{sshos::Key::Char, U'a', sshos::mod::Ctrl}});
  sess.on_input(sshos::InputEvent{sshos::KeyEvent{sshos::Key::Char, U'o', 0}});

  const std::string oob = sess.take_out_of_band();
  CHECK(oob.find("\033[?1002l") != std::string::npos);
  CHECK(oob.find("\033[?1006l") != std::string::npos);
  CHECK(sess.take_out_of_band().empty());  // consommé une seule fois
}

TEST(session_asks_for_a_full_repaint_on_demand) {
  FakePlatform plat;
  sshos::NullFdRegistrar fds;
  Session sess(plat, fds, 80, 24);
  CHECK(!sess.take_repaint());
  sess.on_input(sshos::InputEvent{
      sshos::KeyEvent{sshos::Key::Char, U'a', sshos::mod::Ctrl}});
  sess.on_input(sshos::InputEvent{sshos::KeyEvent{sshos::Key::Char, U'r', 0}});
  CHECK(sess.take_repaint());
  CHECK(!sess.take_repaint());
}
```

- [ ] **Step 2: Lancer, constater l'échec, implémenter**

`LeaderDispatch` tient un booléen `armed_` et la table ci-dessus. `feed()` : si `!armed_`, `Ctrl+<leader>` arme et consomme ; tout le reste passe. Si `armed_`, désarmer d'abord, puis chercher la liaison ; `Ctrl+<leader>` donne `LiteralLeader`. Toute touche armée est consommée, avec ou sans action.

`Menu` construit sa liste à l'ouverture : les entrées du catalogue (`app/catalog.hpp`), puis `panneau: haut / bas / gauche / droite`, puis `battement: battre` et `battement: couper la source`, puis `Quitter la session`. Le filtre est une recherche de sous-chaîne insensible à la casse sur le libellé. Le menu se dessine dans un cadre `View::box`, ancré au bouton de menu du panneau, et **capture le clavier** : tant qu'il est ouvert, les frappes vont au menu, jamais à l'application.

`Session` exécute les actions sur la fenêtre focalisée : les huit déplacements et redimensionnements travaillent sur `user_rect` puis passent par `snap()` ; `Close` suit le chemin de `can_close()` (le dialogue arrive à la tâche 11 ; ici, refuser silencieusement) ; `ToggleMouse` empile `\033[?1002l\033[?1006l` ou `\033[?1002h\033[?1006h` dans `out_of_band_` ; `ForceRepaint` lève `repaint_`.

`LiteralLeader` transmet un `KeyEvent{Key::Char, leader, mod::Ctrl}` à l'application focalisée.

- [ ] **Step 3: Câbler le hors-bande et le repeint dans le démon**

Là où le démon empile une trame, préfixer le hors-bande et honorer le repeint :

```cpp
      if (session.take_repaint()) client->differ->invalidate();
      const std::string oob = session.take_out_of_band();
      client->out.push(encode(Msg{FrameMsg{oob + client->differ->frame(screen)}}));
```

Le client écrit `FrameMsg::ansi` verbatim sur son terminal, et `TtyGuard` restaure les modes DEC à la sortie : une bascule souris laissée active ne survit donc pas à la fin de la session.

- [ ] **Step 4: Tests, sanitizers, commit**

```bash
cmake --build build-release -j"$(nproc)" && ./build-release/sshos_tests
cmake --build build-dbg -j"$(nproc)" && \
  UBSAN_OPTIONS=halt_on_error=1:print_stacktrace=1 ./build-dbg/sshos_tests

git add src/input/shortcuts.hpp src/input/shortcuts.cpp src/shell/menu.hpp \
        src/shell/menu.cpp src/daemon tests/test_shortcuts.cpp \
        tests/test_menu.cpp tests/test_session.cpp
git commit -m "feat(shell): menu de lancement et table de raccourcis leader"
```

Attendu : **298 cas, 0 en echec**.

---

### Task 11: Le dialogue modal, les goldens, la revue de jalon

La dernière tâche ferme la boucle ouverte à la tâche 2 : `Bloc::can_close()` refuse depuis le début, et personne n'avait encore de quoi poser la question à l'utilisateur.

**Files:**
- Create: `src/shell/modal.hpp`, `src/shell/modal.cpp`
- Modify: `src/daemon/session.hpp`, `src/daemon/session.cpp`
- Create: `tests/test_modal.cpp`, `tests/test_golden.cpp`
- Create: `tests/golden/*.txt`

**Interfaces:**
- Produces:
  - `enum class sshos::ModalHit { None, Body, Cancel, Confirm };`
  - `class sshos::Modal` : `void ask(std::string question, WindowId target)`, `void dismiss()`, `bool is_open() const`, `WindowId target() const`, `Rect rect(int cols, int rows) const`, `void draw(View v, const Theme& th, Border b) const`, `ModalHit hit(int x, int y) const`, `void focus_next()`, `bool confirm_focused() const`.

- [ ] **Step 1: Écrire le test qui échoue**

`tests/test_modal.cpp` :

```cpp
// Une seule modale à la fois. Une seconde demande pendant qu'une première
// attend est ignorée : empiler des dialogues sur un bureau texte ne mène
// nulle part, et l'utilisateur ne saurait plus lequel il répond.
TEST(modal_ignores_a_second_question_while_the_first_is_pending) {
  Modal m;
  m.ask("premiere ?", 1);
  m.ask("seconde ?", 2);
  CHECK(m.is_open());
  CHECK_EQ(m.target(), static_cast<sshos::WindowId>(1));
}

TEST(modal_defaults_to_cancel) {
  Modal m;
  m.ask("fermer ?", 1);
  CHECK(!m.confirm_focused());
}
```

Et dans `tests/test_session.cpp`, le chemin complet :

```cpp
// Bloc refuse de se fermer une fois modifiée : la session doit poser la
// question, pas fermer, et pas non plus l'ignorer.
TEST(session_asks_before_closing_a_modified_window_and_honours_the_answer) {
  FakePlatform plat;
  sshos::NullFdRegistrar fds;
  Session sess(plat, fds, 80, 24);
  Surface s(80, 24);
  sess.render(s);

  sess.on_input(sshos::InputEvent{sshos::KeyEvent{sshos::Key::Char, U'z', 0}});

  // Ctrl+A w : demander la fermeture.
  sess.on_input(sshos::InputEvent{
      sshos::KeyEvent{sshos::Key::Char, U'a', sshos::mod::Ctrl}});
  sess.on_input(sshos::InputEvent{sshos::KeyEvent{sshos::Key::Char, U'w', 0}});

  Surface asked(80, 24);
  sess.render(asked);
  bool question = false;
  for (int y = 0; y < 23; ++y) {
    if (asked.text_row(y).find("modifications") != std::string::npos) question = true;
  }
  CHECK(question);

  // Entrée valide le bouton par défaut, qui est Annuler : la fenêtre reste.
  sess.on_input(sshos::InputEvent{sshos::KeyEvent{sshos::Key::Enter, 0, 0}});
  Surface kept(80, 24);
  sess.render(kept);
  CHECK(title_row_of(kept, 24) >= 0);

  // Cette fois on confirme : Tab pour atteindre Confirmer, puis Entrée.
  sess.on_input(sshos::InputEvent{
      sshos::KeyEvent{sshos::Key::Char, U'a', sshos::mod::Ctrl}});
  sess.on_input(sshos::InputEvent{sshos::KeyEvent{sshos::Key::Char, U'w', 0}});
  sess.on_input(sshos::InputEvent{sshos::KeyEvent{sshos::Key::Tab, 0, 0}});
  sess.on_input(sshos::InputEvent{sshos::KeyEvent{sshos::Key::Enter, 0, 0}});

  Surface gone(80, 24);
  sess.render(gone);
  CHECK_EQ(title_row_of(gone, 24), -1);
}

// Une fermeture demandée pendant un glissement annule d'abord le
// glissement. Sans cet ordre, la MAE garderait un identifiant de fenêtre
// détruite et le prochain mouvement de souris irait chercher un fantôme.
TEST(session_cancels_a_drag_before_it_closes_the_window_underneath) {
  FakePlatform plat;
  sshos::NullFdRegistrar fds;
  Session sess(plat, fds, 80, 24);
  Surface s(80, 24);
  sess.render(s);

  press_at(sess, 5, 1);
  motion_to(sess, 8, 6);
  sess.on_input(sshos::InputEvent{
      sshos::KeyEvent{sshos::Key::Char, U'a', sshos::mod::Ctrl}});
  sess.on_input(sshos::InputEvent{sshos::KeyEvent{sshos::Key::Char, U'w', 0}});

  motion_to(sess, 40, 20);
  release_at(sess, 40, 20);
  Surface after(80, 24);
  sess.render(after);  // ne doit ni planter ni déplacer quoi que ce soit
  CHECK_EQ(title_row_of(after, 24), -1);
}
```

- [ ] **Step 2: Implémenter la modale**

`Modal` tient une question, une fenêtre cible et le bouton focalisé. Elle se dessine centrée, dans un cadre, avec la question et deux boutons `[ Annuler ]` `[ Confirmer ]`. **Annuler a le focus par défaut** : la réponse sûre à une question destructrice ne doit jamais être celle qu'on donne par inadvertance.

`Session::hit()` interroge la modale **en premier** et **s'arrête là** si elle est ouverte : rien derrière un dialogue modal n'est cliquable, c'est ce que « modal » veut dire. Le clavier suit la même règle : `Tab` change de bouton, `Échap` annule, `Entrée` valide le bouton focalisé.

Le chemin de fermeture, dans cet ordre :

1. annuler tout glissement en cours ;
2. interroger `app->can_close()` ;
3. si `allowed`, fermer immédiatement — `unwatch_all()` sur l'hôte, puis retirer la fenêtre du gestionnaire ;
4. sinon, ouvrir la modale avec la question rendue par l'application.

- [ ] **Step 3: Écrire le harnais de goldens**

`tests/test_golden.cpp` construit une `Session` de 60×20, joue un scénario scripté, sérialise la surface et compare à un fichier de référence. Deux fichiers par cas : `<nom>.txt` pour les caractères (une ligne par ligne d'écran) et `<nom>.color.txt` pour les couleurs (un couple `fg/bg` par cellule, en index). Les séparer garde le premier lisible à l'œil dans une revue de diff.

Le répertoire se localise sans toucher au `CMakeLists.txt` :

```cpp
// __FILE__ est absolu (CMake passe des chemins absolus), donc le
// répertoire des références se déduit de la position de ce fichier, sans
// dépendre du répertoire courant du lanceur de tests.
std::string golden_dir() {
  std::string p = __FILE__;
  p.resize(p.rfind('/') + 1);
  return p + "golden/";
}
```

Les bordures sont en **ASCII** dans tous les goldens : un cadre Unicode rendrait le fichier de référence illisible dans un diff, et le jeu de caractères est déjà couvert par les tests de `View::box`.

Quand `UPDATE_GOLDEN=1` est présent dans l'environnement, le test **imprime d'abord la différence** puis réécrit la référence. Imprimer avant de régénérer n'est pas cosmétique : une régénération silencieuse transforme n'importe quelle régression en nouvelle vérité.

Huit scénarios : bureau vide ; une fenêtre focalisée ; deux fenêtres qui se recouvrent, celle du dessus focalisée ; une fenêtre réduite avec son entrée de panneau ; une fenêtre maximisée ; le menu ouvert ; la modale ouverte ; le panneau sur le bord gauche.

- [ ] **Step 4: Lancer, régénérer, relire**

```bash
cmake --build build-release -j"$(nproc)"
UPDATE_GOLDEN=1 ./build-release/sshos_tests golden
./build-release/sshos_tests golden
```

**Relire les huit fichiers à l'œil avant de les commiter.** Un golden généré n'est pas une référence : il devient une référence quand quelqu'un a confirmé que ce qu'il décrit est bien ce qu'on voulait voir.

- [ ] **Step 5: La revue de jalon**

```bash
cmake --build build-release -j"$(nproc)" && ./build-release/sshos_tests
cmake --build build-dbg -j"$(nproc)" && \
  UBSAN_OPTIONS=halt_on_error=1:print_stacktrace=1 ./build-dbg/sshos_tests

for i in $(seq 1 20); do
  ./build-release/sshos_tests daemon_ | tail -1
done
```

Puis, à travers SSH, dérouler la liste : ouvrir deux fenêtres par le menu, en déplacer une à la souris, en redimensionner une par le coin, réduire, maximiser, passer en plein écran, revenir, déplacer le panneau sur chacun des quatre bords, ouvrir Battement et le faire battre, taper dans Bloc puis tenter de la fermer et répondre aux deux boutons, rétrécir le terminal sous 40×12 et le rétablir, détacher et se rattacher.

Vérifier enfin que le démon **ne consomme rien au repos** : la sonde CPU du round EPOLLHUP doit toujours relever 0 jiffie sur 2 s, bureau chargé compris.

- [ ] **Step 6: Commit**

```bash
git add src/shell/modal.hpp src/shell/modal.cpp src/daemon \
        tests/test_modal.cpp tests/test_golden.cpp tests/golden
git commit -m "feat(shell): dialogue modal, fermeture negociee et references de rendu"
```

Attendu : **311 cas environ, 0 en echec**.

---

## Ce que le jalon 2 ne fait pas

À rappeler dans le rapport de fin de jalon, pour que personne ne le cherche :

- **Aucun PTY.** `Platform::spawn()`, `TIOCSWINSZ`, le parseur VT, le scrollback et la souris transmise aux invités appartiennent au jalon 3.
- **Aucun fichier de configuration.** Le parseur INI du §12 de la conception maître est hors périmètre : le bord du panneau, le thème et la touche leader se changent au menu ou au raccourci, pas dans un fichier.
- **Aucun accord `Alt`.** La table de raccourcis passe entièrement par le leader.
- **Aucune aide de découvrabilité.** Le `<leader>` seul n'affiche pas la liste des liaisons possibles.

Les deux applications factices **restent au catalogue** après le jalon 3. Ce sont elles qui gardent vivants les chemins du contrat applicatif — `on_io`, `can_close`, `set_title`, le compteur de `on_resize` — que de vraies applications exerceraient sans jamais les rendre observables.
